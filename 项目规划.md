一、 项目概述
本项目是一个基于微信生态的“知识管理 + 社交日程”小程序。它不仅仅是一个日历，而是融合了日程协作、碎片化知识获取（每日一言）、深度学习内化（Anki记忆卡片）的个人成长手账系统。
产品核心在于：深度利用微信群聊社交生态，以及高度整合的模块间数据联动（日历作为数据中心）。
二、 技术栈选型
● 前端框架：uni-app (Vue3 + <script setup> 语法)
● 后端/数据库：uniCloud (阿里云/腾讯云 Serverless 云开发)
● 核心UI组件：uni-ui (主要依赖 uni-calendar、原生 <swiper>)
● 目标平台：微信小程序

三、 核心功能模块与技术实现路线
模块一：微信社交与共享日程 
打破传统单机日历的孤岛，实现基于微信群聊的轻量化日程协作。
● 功能描述：
  ○ 共享日程：创建诸如“宿舍值日表”、“团队项目计划”、“家庭出行安排”等日程。
  ○ 活动报名与统计：发起“周末剧本杀”日程，支持群成员点击“参加”，系统自动统计参与人数及名单。
● 技术实现：
  ○ 利用微信小程序的 onShareAppMessage API，将带有 schedule_id 参数的卡片分享到微信群。
  ○ 其他用户点击卡片进入小程序，解析 schedule_id，触发 uniCloud 云函数往参与者列表中 push 该用户的 openid。
  ○ 数据实时刷新，利用云数据库的原子操作确保高并发下的报名统计准确性。
模块二：聚合版“每日一言” 
碎片化知识的输入源，支持沉浸式浏览与外部阅读延伸。
● 功能描述：
  ○ 卡片滑动切换：左右滑动无缝浏览不同来源的金句。一键跳转《单向历》小程序。
● 技术实现：
  ○ 深度阅读跳转：当句子关联某本书籍时，提供按钮一键跳转至《微信读书》
  ○ UI呈现：使用原生 <swiper> 组件包裹卡片，实现丝滑切换。
  ○ 数据聚合：在云函数中使用 Promise.all 同时请求多个第三方 API，将数据清洗后整合为一个数组（Array）返回给前端 <swiper> 绑定。
  ○ 跳转逻辑：调用 uni.navigateToMiniProgram API，配置目标小程序的 AppID 和特定 path 实现跨小程序引流。
模块三：Anki式记忆卡片 
将碎片化知识转化为深度记忆的内化工具。
● 功能描述：
  ○ 大卡片交互：下方区域展示大尺寸复习卡片，支持左右滑动切换题目。
  ○ 自测与比对：正面展示题目，支持通过键盘（<textarea>）输入答案；点击翻面后，展示标准答案并与用户输入的内容进行对比。
  ○ 极简批量导入：支持通过文本格式（如 正面内容 --- 背面内容）或 CSV 文件一键导入成百上千张卡片。
● 技术实现：
  ○ 3D翻转动画：基于 CSS3 属性实现。外层容器设置 perspective，正反面卡片绝对定位，点击时给容器动态添加类名，触发 transform: rotateY(180deg) 和 backface-visibility: hidden。
  ○ 批量解析：前端提供一个大的文本输入框，利用 JavaScript 字符串处理（split('---') 和正则表达式），将文本按行切分后批量写入 uniCloud 数据库。
  ○ 图片支持：卡片支持图片展示，利用 uniCloud 云存储上传和读取图片URL。
模块四：日历数据中枢 
整个小程序的“大脑”，连接所有零散模块的联动中心。
● 功能描述：
  ○ 收藏联动：在每日一言卡片点击“收藏”，该动作会直接在日历的对应日期上打上标记。
  ○ 时光机回顾：打开日历弹窗，点击过往的任意一天，下方的卡片会自动刷新为当天的日程、收藏的金句和打卡的复习题。
● 技术实现：
  ○ 写入逻辑：点击收藏时，调用云函数向 favorites 表插入记录，包含字段 date: "2024-10-28"。
  ○ 标记逻辑 (打点)：日历组件加载时，查询 favorites 和 schedules 表，提取所有存在的日期，组装成 uni-calendar 要求的 marks / selected 数组格式，渲染出日期下方的小红点。
  ○ 查询联动：监听 uni-calendar 的 @confirm 事件，获取选中的 e.fulldate，将其作为参数派发给“每日一言”和“Anki卡片”组件，重新发起数据库查询。

四、 数据库架构简述 (uniCloud DB)
为支撑以上逻辑，需建立以下核心数据表（Collections）：
1. schedule_events (日程表)
  ○ _id: 唯一标识
  ○ title: 日程标题 (剧本杀)
  ○ date: 日期 ("2024-10-28")
  ○ creator_id: 发起人
  ○ participants: [参与者 openid 数组] (用于统计人数)
2. user_favorites (收藏表)
  ○ user_id: 用户ID
  ○ date: 收藏日期
  ○ content: 金句内容 / 来源
3. flashcards (记忆卡片表)
  ○ deck_id: 所属牌组/类别
  ○ front_content: 正面问题 (文本/图片URL)
  ○ back_content: 背面答案
  ○ review_date: 下次复习日期 (进阶 Anki 算法使用)

五、 敏捷开发路线图 (MVP 建议)
由于项目功能丰富，建议分阶段进行开发以避免延期：
● Phase 1：个人基础工具版 (本周目标)
  ○ 实现带日历弹窗的首页 UI。
  ○ 实现 <swiper> 聚合 API 每日一言。
  ○ 实现点击收藏 -> 本地缓存/云端存入 -> 日历打点的基础联动。
● Phase 2：硬核学习版 (下周目标)
  ○ 实现 Anki 卡片 UI（3D 翻转 + Textarea 输入）。
  ○ 写一段 JS 脚本，跑通“文本格式批量导入到数据库”的流程。
● Phase 3：微信社交裂变版 (最终目标)
  ○ 接入微信登录体系，获取 OpenID。
  ○ 开发“新建活动日程”表单。
  ○ 处理微信群聊分享（ShareTicket）和点击加入的并发逻辑。