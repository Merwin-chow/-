# 整改方案基础测试方案与用例

> 版本：v1.1 ｜ 日期：2026-08-16
> 配套文档：《云函数与数据库设计分析.md》《云函数与数据库整改方案.md》
> 用途：为整改方案（Phase 0~8）设计基础测试，作为整改验收与回归的依据。
> 测试对象：`calendar/` 前端 + `uniCloud-aliyun/` 云函数与数据库。
> v1.1：新增「调用效率/定时」复核项用例（T-410/511/512/513/602b/605），对应整改方案 1.4 与第四章抉择。

---

## 一、测试目标与范围

验证整改方案是否达到以下目标：

| 目标 | 对应问题 |
|---|---|
| 身份稳定：登录/重启后 uid 不变，数据不再按 token 归档 | P0-1 |
| 服务端鉴权：伪造/越权 user_id 无效 | P0-2 |
| 数据库封闭：客户端无法直查他人数据 | P0-3 |
| 业务正确：日历打点、搜索、私有日程、名单权限正确 | P0-4/5、P1-6 |
| 并发一致：报名不超名额、收藏/加入/备注不重复不丢 | P1-7/8 |
| 提醒可靠：发送失败可重试，不丢提醒 | P1-9 |
| 配置治理：无硬编码密钥/模板 ID | P1-10 |
| 存量迁移：token 哈希数据正确映射到 uid | P0-1(存量) |
| 调用效率：今日诗词按日单次回源、收藏批量检测、打点按月 | v1.1 新增（方案 1.4） |
| 定时正确：单字段 `next_remind_at` 索引、超量提醒不遗漏 | v1.1 新增（方案 4.2） |

---

## 二、测试环境与工具

| 项目 | 说明 |
|---|---|
| 开发环境 | HBuilderX（含「运行到小程序模拟器」「云函数本地运行」） |
| 云环境 | uniCloud 阿里云开发空间（**独立于正式空间**，避免污染生产数据） |
| 工具 | uniCloud 控制台（数据查看/索引/日志）、`JQL查询.jql`（仅服务端/工具端调试）、微信开发者工具 |
| 账号准备 | 微信测试账号 ×3（A 用户、B 用户、C 用户）、游客模式 |
| 数据准备 | 每个用例使用独立测试数据前缀，如收藏内容 `[T01]xxx`，便于清理 |

> 所有「越权类」用例必须使用**两个以上真实账号**验证，仅靠游客模式无法覆盖。

---

## 三、测试策略分层

| 层 | 方式 | 用例数 |
|---|---|---|
| 云函数层（单元/接口） | HBuilderX 直接调用云函数，构造 event 参数 | ~30 |
| 前端集成层 | 小程序模拟器 + 真实账号走完整流程 | ~25 |
| 并发层 | 脚本并发调用云函数（报名/收藏/加入） | 3 |
| 安全层 | 伪造 token、越权 uid、客户端 JQL 直查 | 8 |
| 迁移层 | 一次性云函数 dryRun / run | 5 |
| 回归层 | 全模块冒烟 | 1 套 |

---

## 四、测试用例清单

### 4.1 Phase 1 身份与鉴权

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-101 | P0 | 云函数 login 已重构 | 用户 A 首次 wxLogin | 返回 code:200，data 含 uid/token；`uni-id-token` 新增 1 条 |
| T-102 | P0 | T-101 | 用户 A 再次 wxLogin | uid 不变；`uni-id-token` 中 A 仅 1 条（旧 token 被清理） |
| T-103 | P0 | 登录态 | 重启小程序，`getUid()` 与上次一致 | 收藏/卡片/日程按 uid 正常加载 |
| T-104 | P0 | — | 无 token 调用 `login.getCurrentUser` | code:401 |
| T-105 | P0 | 登录态 | 篡改 token（改 1 位）调 getCurrentUser | code:401（uni-id-common checkToken 拒绝） |
| T-106 | P0 | 登录态 | 用 A 的 token 调 `toggle_favorite.list`，但 event 里传 `user_id: B` | 返回 A 的数据，而非 B |
| T-107 | P0 | 登录态 | `login.phoneLogin` 传正确 token + phone | 手机号写入 A 的 uni-id-users.phone |
| T-108 | P1 | 登录态 | `login.updateProfile` 不带 token 传任意 uid | code:401，档案未改 |
| T-109 | P1 | — | 游客模式调 `toggle_favorite.add` | code:401（写操作需登录），前端跳登录 |

### 4.2 Phase 2 数据库权限与索引

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-201 | P0 | schema 已收紧 | 小程序端执行 `db.collection('user_favorites').get()` | 无权限错误，返回失败 |
| T-202 | P0 | schema 已收紧 | 小程序端执行 `db.collection('uni-id-users').get()` | 失败（无法读到他人 PII） |
| T-203 | P1 | 控制台已建索引 | `explain` 查看 `user_favorites(user_id,date,content)` 查询 | 走索引，非 COLLSCAN |
| T-204 | P1 | 唯一索引已建 | 插入重复 (user_id,date,content) 收藏 | 唯一索引拒绝/云函数捕获为「已收藏」 |

### 4.3 Phase 3 核心业务缺陷

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-301 | P0 | 登录态 | 创建个人日程后打开日历 | 对应日期出现「日程」红点 |
| T-302 | P0 | 登录态 + 群 | 报名群日程后打开日历 | 对应日期出现「报名」红点 |
| T-303 | P0 | 登录态 | 收藏一句话后打开日历 | 对应日期出现「已收藏」红点 |
| T-304 | P1 | 登录态 | 切换日历月份 | 仅当月打点请求，性能正常 |
| T-305 | P0 | search 已注册+改走云函数 | 进入搜索页搜索「李白」 | 同时返回卡片与收藏结果 |
| T-306 | P0 | 登录态 | 搜索他人收藏中的关键词 | 不返回他人收藏 |
| T-307 | P1 | 游客态 | 打开搜索页 | 可搜索，写操作提示登录 |

### 4.4 Phase 4 权限边界与敏感信息

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-401 | P0 | A 创建私有日程 | 用 B 的 token 访问该 schedule_id 详情 | code:403 |
| T-402 | P0 | A 创建私有日程 | 用 A 的 token 访问详情 | code:200 |
| T-403 | P0 | 群报名活动 + C 报名 | B(普通成员) 调 `schedules.signups` | 200，名单无 `profile_phone`/生日，phone 脱敏 |
| T-404 | P0 | 同上 | 群主调 `schedules.signups` | 200，名单含手机号/生日 |
| T-405 | P0 | 非群成员 | D 调该活动 `signups` | code:403 |
| T-406 | P0 | A 建卡片 | B 的 token 调 `flashcards.updateStatus` 改 A 的 card_id | code:403 或 updated=0 |
| T-407 | P0 | A 建卡片 | B 的 token 调 `flashcards.delete` 删 A 的 card_id | 删除失败（card_id 不存在或 403） |
| T-408 | P1 | — | 群外用户调 `groups.detail` | code:403 |
| T-409 | P1 | — | 普通成员调 `groups.setRemark` 改他人 | code:403 |
| T-410 | P0 | A 创建私有日程 | B 的 token 调 `schedules.history`（schedule_id=A 的日程） | code:403；A 本人与群成员可读历史 |

### 4.5 Phase 5 一致性 / 并发 / 提醒

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-501 | P0 | 报名活动 quota=50 | 脚本并发 200 次报名 | 成功数 = 50，第 51 个起「名额已满」；`signup_count`=50；`signup_closed=true` |
| T-502 | P0 | 场次名额=10 | 并发 50 次报名同一场次 | 该场次仅 10 人，其余报总名额或失败 |
| T-503 | P0 | 报名满员后 | 某人 `cancelSignup` | `signup_count` 减 1，`signup_closed` 重新开放（若未满） |
| T-504 | P0 | — | 并发收藏同一内容 50 次 | `user_favorites` 仅 1 条 |
| T-505 | P0 | 群 B/C | A 与 B 同时 join 同群 | members 中 A/B 各 1 条，无重复 |
| T-506 | P1 | 群 | A 群主 + B 同时改 C 的备注 | 两处备注不相互覆盖（事务生效，以最后一次提交为准且不丢字段） |
| T-507 | P0 | 日程 reminders 未发 | 手动调用 `schedule_notify`（模拟发送失败：模板 ID 填错） | `reminders.sent` 保持 false；修正模板后重跑发送成功 |
| T-508 | P0 | 发送成功后 | 再次调用 `schedule_notify` | 不重复发送（已 sent） |
| T-509 | P1 | 生日用户 | 手动调用生日触发器 | 当天生日用户被通知，`group_events` 有 `birthday_notify` 且次日不重复 |
| T-510 | P1 | 到期日程 >100 | 造 150 条待提醒 | 全部在后续轮次内被处理，无遗漏 |
| T-511 | P1 | 收藏 200 条 | 前端渲染首页（触发 `checkBatch`） | 一次渲染仅 1~2 次云函数调用，`isFavorited` 全部正确 |
| T-512 | P1 | 跨月收藏/日程 | 调 `getMarks` 分别传当月/上月 | 当月含当月数据，上月不含当月；均只查当月 |
| T-513 | P1 | 当日缓存未命中 | 并发 10 次调 `get_daily_quote` | 外部 API 当日只回源 1 次（单飞生效），10 次均返回 200 |

### 4.6 Phase 6 配置治理与可维护性

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-601 | P1 | 配置迁移完成 | grep 云函数目录 | 无 `TIANAPI_KEY`/`appsecret`/模板 ID 硬编码 |
| T-602 | P1 | get_daily_quote 代理今日诗词 | 关闭前端直调，走云函数 | 首页诗词正常展示 |
| T-602b | P1 | 今日诗词代理上线 | 连续进入首页/切换日期 5 次 | `get_daily_quote` 当日日志中 jinrishici 只回源 1 次；前端无 `uni.request` 直调 |
| T-603 | P1 | schedules 拆分 | 逐一执行 create/update/delete/signup/detail/history | 与原行为一致 |
| T-604 | P2 | 批量导入 | 一次导入 200 张卡片 | 1 次云函数调用完成，200 条入库 |
| T-605 | P1 | 定时扫描 | 查看 `schedule_notify` 索引 | 存在单字段 `next_remind_at` 索引（非复合） |

### 4.7 Phase 7 数据迁移

| 用例ID | 级别 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|---|
| T-701 | P0 | 迁移脚本就绪 | `migrate_user_id` dryRun=true | 输出各表将替换/去重条数，与预估值一致 |
| T-702 | P0 | 备份完成 | dryRun=false 执行 | 无 64 位 hex 残留于 user_id/owner_id 字段（除 uni-id-token.token） |
| T-703 | P0 | 迁移后 | 用户 A 登录 | 历史收藏/卡片/日程可见（uid 已统一） |
| T-704 | P0 | 迁移后 | 检查 `uni-id-token` | 每用户仅 1 条 |
| T-705 | P1 | 迁移后 | 检查唯一索引覆盖字段 | 无重复（user_favorites/signups/invite_code） |

### 4.8 回归冒烟（全模块）

| 用例ID | 级别 | 步骤 | 预期结果 |
|---|---|---|---|
| T-801 | P0 | 登录→首页一言→收藏→日历打点→记忆卡片→建群→建日程→发起报名→报名→名单→提醒 | 全链路无阻断性缺陷，均通过 |

---

## 五、并发测试脚本示例（T-501）

在云函数中临时添加受控测试入口或使用命令行工具循环调用：

```js
// 并发报名脚本（Node，循环调用云函数 signup）
const concurrency = 200
const tasks = Array.from({ length: concurrency }, (_, i) =>
  callSignup(scheduleId, `user_${i}`)  // 各自独立 token
)
const results = await Promise.allSettled(tasks)
const ok = results.filter(r => r.status === 'fulfilled' && r.value?.code === 200).length
console.log(`成功报名: ${ok}（应为 50）`)
```

> 生产环境请勿直接注入用户；测试在开发空间进行，user_xxx 为迁移前临时游客 ID 或测试账号。

---

## 六、通过标准与缺陷分级

| 级别 | 定义 | 处理 |
|---|---|---|
| 阻断 (Blocker) | 数据丢失/泄露/功能不可用（如 401 风暴、越权读手机号） | 修复后重测全回归 |
| 严重 (P0) | 核心流程错误（打点缺失、并发超名额、数据失联） | 修复后重测相关用例 |
| 一般 (P1) | 权限/边界/提醒可靠性缺陷 | 本版本内修复 |
| 轻微 (P2) | 性能/体验优化项 | 可后续迭代 |

**总体通过标准**：
- Blocker = 0，P0 缺陷 = 0；
- 并发用例通过（T-501~T-506）；
- 安全用例全部通过（4.2、4.4）；
- 迁移用例全部通过且 dryRun 报告与预期一致。

---

## 七、自动化可行性建议

1. **云函数接口测试**：云函数可被直接调用，可封装为 `test/cloud-api.test.js` 在 Node 环境用 `uniCloud` 客户端 SDK 驱动，纳入 CI。
2. **并发用例**：脚本化后入库 `test/concurrency.js`，每次发布前跑一遍。
3. **前端回归**：接入微信小程序自动化（miniprogram-automator）覆盖 T-801 主链路。
4. **迁移用例**：先构建「污染库」（手工插入 token 哈希数据）再跑 dryRun/run，比对结果。

---

> 本方案为基础测试集，覆盖整改方案的每个验收标准；实施整改时按 Phase 顺序执行对应用例。
