# 云函数与数据库整改方案

> 版本：v1.0 ｜ 日期：2026-08-16
> 前置文档：《云函数与数据库设计分析.md》（问题编号沿用该文档 P0/P1/P2 分级）
> 范围：`calendar/` 前端与 `uniCloud-aliyun/` 云函数、数据库、配置
> 原则：**先架构梳理，再分阶段落地；每步可独立上线、可回滚、可验证**

---

## 一、整改目标与总体架构梳理

### 1.1 现状问题归类（引用分析文档）

| 层级 | 根因 | 问题编号 |
|---|---|---|
| 身份 | 前端把 `token`（每次登录变化）当 `user_id` 用，业务数据按 token 哈希归档 | P0-1 |
| 鉴权 | 云函数信任客户端 `event.user_id`，无 token 校验 | P0-2 |
| 数据库 | 所有 schema `permission` 全开，客户端 JQL 可直读他人 PII | P0-3 |
| 业务 | `getMarks` 用错字段、搜索页调用不存在云函数、私有日程/报名名单越权 | P0-4/5、P1-6 |
| 一致性 | 报名名额非原子、查重非原子、发送失败丢提醒 | P1-7/8/9 |
| 治理 | 密钥硬编码、模板 ID 散落、`schedules` 单体 947 行、无索引 | P1-10、P2 |

### 1.2 目标架构总览

```
┌────────────────────────────── 前端 pages/ ──────────────────────────────┐
│  utils/auth.js    getUid()/getToken()/requireLogin()   统一身份获取      │
│  utils/cloud.js   callApi(fn, action, data)  自动注入 token，统一 401 处理 │
│  所有数据读写：uniCloud.callFunction(...)  （禁止客户端 db 直查）         │
└──────────────┬───────────────────────────────────────────────────────────┘
               ▼  callFunction({ token, uid, action, ...data })
┌────────────────────────────── 云函数层 ─────────────────────────────────┐
│  login          登录/档案/登出（接入 uni-id-common）                     │
│  get_daily_quote 每日内容聚合 + 缓存（key 收敛到配置中心）               │
│  toggle_favorite 收藏 + 日历打点 + 收藏搜索                              │
│  flashcards      记忆卡片 CRUD + 批量导入                                │
│  groups          群组协作（join 原子化、setRemark 原子化、disband 事务）  │
│  schedules       日程 + 报名 + 草稿 + 历史（内部按子域拆分文件）          │
│  schedule_notify 定时提醒/生日（发送成功才置 sent，分批处理）            │
└──────────────┬───────────────────────────────────────────────────────────┘
               ▼  uniCloud.database() / db.startTransaction()
┌────────────────────────────── 数据库层 ─────────────────────────────────┐
│  schema.permission 全部收紧（客户端零直查，读写全走云函数）              │
│  索引完整 + 唯一约束（openid / invite_code / (schedule_id,user_id) 等）  │
│  身份字段统一 = uni-id-users._id（uid），token 只用于鉴权                │
└──────────────────────────────────────────────────────────────────────────┘

公共模块（uniCloud-aliyun/cloudfunctions/common/）：
  uni-id-common    官方 JWT token 生成/校验（uni_modules 已内置）
  uni-config-center 密钥/模板ID/AppID 集中管理（uni_modules 已内置）
  uni-subscribemsg  订阅消息（已在使用）
```

### 1.3 关键架构决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 身份标识 | 业务全部改用 `uid = uni-id-users._id`；token 仅作鉴权凭据 | 稳定、跨登录不变，与文档 `user_id` 语义一致 |
| token 体系 | 接入 `uni-id-common`（JWT），废弃自研 SHA256 token | 官方成熟、支持过期/校验，`uni_modules` 已内置 |
| 数据访问 | schema `permission` 全 `false`，读写全部走云函数 | 从根上杜绝客户端越权直查 |
| 并发控制 | 报名/场次用**事务** + `signup_count` 原子字段；`setRemark` 用条件更新 | 保证"先查后写"不超卖 |
| 提醒可靠性 | 发送成功后才置 `reminders.sent=true`；失败保留待重发 | 修复"丢提醒" |
| 配置管理 | 密钥/模板 ID/AppID 统一放入 `uni-config-center` 或云函数环境变量 | 修复硬编码 |
| 拆分策略 | `schedules` 先拆内部文件，保持单入口；不再新增 action 时考虑拆云函数 | 平衡改动成本与可维护性 |
| 今日诗词来源 | 云函数代理 + 按日缓存（替代前端直调） | 消除每次进首页的重复抓取；token 移出小程序包；与 `daily_data` 共用缓存实现"一次抓取多次复用" |
| 收藏状态检测 | 新增 `checkBatch` 批量接口（替代逐卡 check） | 每页 5~10 张卡片由 N+1 次调用收敛为 1 次 |
| 日历打点范围 | `getMarks` 增加 `month` 参数（月度查询，替代全量） | 打点只关心当月，消除每次挂载/切月/收藏后的全表扫描 |
| 报名数读取 | 以 `signup_count` 字段为准（替代聚合查询） | 写路径原子维护、读路径 O(1)，消除 `cmd.in` 上限与重复聚合 |
| 定时粒度 | 保持每 5 分钟扫描 | 提醒提前量以 10~60min 为主，5min 粒度足够；改用单字段 `next_remind_at` 索引降低扫描成本 |

---

## 二、分阶段整改路线图

| 阶段 | 目标 | 对应问题 | 预计改动 |
|---|---|---|---|
| Phase 0 | 数据快照、基线提交 | — | 导出全部集合 + git tag |
| Phase 1 | 身份与鉴权体系 | P0-1/2 | `login` 云函数 + 前端 `utils/` + 各云函数入口 |
| Phase 2 | 数据库权限与索引 | P0-3 | 全部 `.schema.json` + 控制台索引 |
| Phase 3 | 核心业务缺陷 | P0-4/5 | `toggle_favorite`、`search.vue`、`pages.json` |
| Phase 4 | 权限边界与敏感信息 | P1-6 | `schedules.detail/signups`、`flashcards` |
| Phase 5 | 一致性/并发/提醒可靠性 | P1-7/8/9 | 事务、原子更新、`schedule_notify` |
| Phase 6 | 配置治理与可维护性 | P1-10、P2 | `uni-config-center`、`schedules` 拆分、批量导入、级联清理 |
| Phase 7 | 数据迁移（token→uid） | P0-1 存量 | 一次性迁移云函数 |
| Phase 8 | 回归验证与上线 | — | 测试清单 + 灰度 |

> 每个 Phase 完成即独立可上线；Phase 7（存量迁移）建议在 Phase 1 代码就绪后、正式发布前执行。

### 1.4 现有设计的「抓取/调用」与「定时」复核（本方案新增分析）

> 复核对象：`get_daily_quote`（外部 API 抓取）、`index.vue`（前端直调）、`attachSignupCounts`/`checkFavoriteStatus`/`getMarks`（重复查询）、`schedule_notify`（定时器）。

#### 1.4.1 一次抓取→存储→多次复用：当前是否存在重复抓取/调用

| 数据 | 来源 | 现状 | 问题（重复抓取/调用） | 本方案对策 |
|---|---|---|---|---|
| 天行AI/新闻/历史 + hitokoto | 外部 HTTP（`get_daily_quote`） | 按 `date` 写 `daily_data` 缓存，命中不抓取 | ✅ 已实现"一次抓取多次复用"；但并发首访可重复抓取（无单飞锁），且历史日期永不过期 | Phase 6.4 增加 `expireAt` + 清理孤儿缓存 |
| 今日诗词 | 前端 `uni.request` 直调（`index.vue:235-258`） | 每次 `loadData` 都请求一次，**无任何缓存** | ❌ **重复抓取**：进入首页、切换日期均重新请求；同一日期内容不稳定；token 随包分发 | 改为云函数代理 + 按日缓存（Phase 6.7），并更新《开发方案思考.md》 |
| 日程报名数 | `signups` 聚合（`schedules/index.js:326`） | 每次 `getMonth`/`detail`/`getGroupSchedules` 都重算 | ❌ 重复聚合 + `cmd.in` 上限风险 | 用 `signup_count` 字段读取（Phase 5.1） |
| 收藏状态 | `toggle_favorite.check` | 首页对每张卡片逐个调用 | ❌ N+1：`checkFavoriteStatus` 对每卡一次云函数调用（`index.vue:338-356`） | 新增 `checkBatch`（Phase 3.5） |
| 日历打点 | `toggle_favorite.getMarks` | 全量拉取收藏+日程日期，挂载/切月/收藏后各一次 | ⚠️ 无月份过滤，重复全表扫描 | 增加 `month` 参数（Phase 3.2） |

#### 1.4.2 定时设置复核

| 定时器 | Cron（7 字段） | 当前行为 | 问题 | 本方案对策 |
|---|---|---|---|---|
| 日程提醒 `schedule_notify_timer` | `0 */5 * * * * *`（每 5 分钟） | 扫 `next_remind_at ≤ now` 的日程 | ⚠️ ① 查询条件只有 `next_remind_at`，**方案 2.2 的 `(date, next_remind_at)` 复合索引用不上**，应为单字段 `next_remind_at` 索引；② `limit(100)` 无排序/无游标，超量延迟；③ 发送失败先置 sent（P1-9） | 5.5 发送成功才置 sent + 游标续拉；2.2 索引修正；6.6 超时/批量 |
| 生日提醒 `birthday_notify_timer` | `0 0 9 * * * *`（每天 9:00） | 正则扫 `birthday` 全表 | ⚠️ 正则无索引、`limit(100)` 会漏 | 5.6 派生字段 `birthday_mmdd` + 分批 |
| 并发/超时 | `concurrency:1` / `timeout:10s` | 串行，防重入 | ✅ 合理；但单轮 100 条 × 群成员订阅发送可能 >10s | 5.5 分批 + 6.6 超时上调 |

**复核结论**：外部 API 主链路已做到"一次抓取多次复用"；**重复抓取/调用的重灾区是前端直调今日诗词与收藏状态 N+1**，以及无月份过滤的 `getMarks`；定时粒度 5 分钟合理，但查询条件与索引不匹配需修正。

---

---

## 三、详细整改步骤

---

### Phase 0：数据快照与基线

**目的**：为所有改动提供回滚依据。

1. 登录 uniCloud 控制台（阿里云空间）。
2. 对以下集合执行「导出」备份：`uni-id-users`、`uni-id-token`、`daily_data`、`user_favorites`、`flashcards`、`groups`、`group_events`、`schedules`、`schedule_history`、`signups`、`signup_drafts`。
3. `git tag release-before-rework && git commit`（先确认当前工作区可提交或另建分支 `rework`）。
4. **验收**：备份文件齐全、可重新导入。

---

### Phase 1：身份与鉴权体系（P0-1 / P0-2）

#### 1.1 前端：统一身份获取工具

**问题**：各页面 `getUserId()` 优先返回 `uni_id_token`（token 哈希），token 每次登录变化 → 数据失联。

**新增** `calendar/utils/auth.js`：

```js
export function getUid() {
  const uid = uni.getStorageSync('current_user_id')
  if (uid) return uid                       // 登录态：稳定 uid
  const token = uni.getStorageSync('uni_id_token')
  if (token) return null                    // 有 token 无 uid：异常，需刷新登录
  const visitorId = uni.getStorageSync('visitor_id') || genVisitorId()
  return 'visitor_' + visitorId             // 游客态
}
export function getToken() {
  return uni.getStorageSync('uni_id_token') || ''
}
export function genVisitorId() {
  const id = 'v_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8)
  uni.setStorageSync('visitor_id', id)
  return id
}
export function requireLogin(targetPath) {
  if (getToken() && uni.getStorageSync('current_user_id')) return true
  uni.navigateTo({ url: `/pages/login/login?redirect=${encodeURIComponent(targetPath)}` })
  return false
}
```

**新增** `calendar/utils/cloud.js`（统一调用 + token 注入 + 401 处理）：

```js
import { getToken } from './auth.js'
export function callApi(name, action, data = {}) {
  const token = getToken()
  return uniCloud.callFunction({
    name,
    data: { token, uid: uni.getStorageSync('current_user_id') || '', action, ...data }
  }).then(res => {
    const r = res && res.result
    if (r && r.code === 401) {
      uni.removeStorageSync('uni_id_token')
      uni.removeStorageSync('current_user_id')
      uni.navigateTo({ url: '/pages/login/login' })
    }
    return r
  })
}
```

**改动页面**（10 个）：把各自 `getUserId()` 统一替换为 `import { getUid } from '@/utils/auth.js'` 并改用 `callApi`：
`pages/index`、`favorites`、`anki`、`create-card`、`schedule`、`create-schedule`、`schedule-detail`、`groups`、`group-detail`、`signup-create`、`search`、`login`。

**验收**：
- 登录后 `getUid()` 恒返回同一 uid（重启小程序后不变）。
- 重新登录后收藏/卡片/日程/群仍能正常加载（新数据层面）。
- 断网/未登录仍走游客分支。

#### 1.2 云函数 login：接入 uni-id-common

**改动文件**：
- `uniCloud-aliyun/cloudfunctions/login/package.json`：dependencies 增加
  `"uni-id-common": "file:../common/uni-id-common"`
- `uniCloud-aliyun/cloudfunctions/login/index.js`：整体重构

**目标行为**：

```js
const uniIdCommon = require('uni-id-common')
exports.main = async (event, context) => {
  const uniID = uniIdCommon.createInstance({ context })  // 读取 uni-id.config.json
  const db = uniCloud.database()

  // wxLogin：jscode2session → 查/建用户 → uniID.createToken({ uid }) → 清理旧 token → 写 uni-id-token
  // getCurrentUser：uniID.checkToken(event.token) → 校验通过 → 按 uid 查档案
  // updateProfile：uniID.checkToken → 校验通过 → 按 uid 更新（不再信任 event.uid）
  // phoneLogin：uniID.checkToken → 校验通过 → 按 uid 绑定手机号（修复 doc(event.token) 的错误）
}
```

关键点：
- `uniID.createToken({ uid })` 返回 JWT，`uniID.checkToken(token)` 返回 `{ errCode: 0, uid }` 或 `{ errCode: 'uni-id-token-expired', code: 30203 }`。
- 登录成功后**先删除该用户旧 token**（`uni-id-token.where({ user_id: uid }).remove()`）再新增，防止 token 表无限增长。
- 返回结构保持 `{ code: 200, data: { uid, token, nickname, avatar, birthday, isNew } }`，**前端无需改动存储逻辑**。
- 校验失败统一返回 `{ code: 401, message: '登录已失效' }`，供 `callApi` 统一跳登录。
- `uni-id.config.json` 中 `tokenSecret`/`passwordSecret` 替换为随机强密钥（Phase 6 一并处理）。

**验收**：
- 同一用户连续登录两次，`uni-id-token` 中只保留 1 条 token。
- 伪造/篡改 token 调用 `getCurrentUser` 返回 401。
- `phoneLogin` 能正确绑定手机号到对应用户。

#### 1.3 云函数：统一鉴权解析 uid

**目标**：所有云函数不再信任 `event.user_id`，改为从 `event.token` 解析出真实 `uid`。

**推荐方案（最小侵入）**：每个云函数入口加一个本地 `resolveUid`：

```js
const uniIdCommon = require('uni-id-common')
async function resolveUid(context, event) {
  const token = event.token
  if (!token) {
    // 游客：允许 visitor_ 前缀的只读/轻量操作
    const uid = event.uid || ''
    return uid.startsWith('visitor_') ? { uid, isVisitor: true } : { code: 401 }
  }
  const uniID = uniIdCommon.createInstance({ context })
  const res = await uniID.checkToken(token)
  if (res.errCode) return { code: 401, message: '登录已失效' }
  return { uid: res.uid, isVisitor: false }
}
```

接入清单（7 个云函数）：
- `login`：如上重构。
- `get_daily_quote`：公开接口，不校验 token；缓存读取不区分用户。
- `toggle_favorite` / `flashcards` / `groups` / `schedules`：入口即 `resolveUid`，后续所有 `user_id`/`owner_id` 均取解析出的 `uid`，**忽略 `event.user_id`**；游客（`isVisitor`）在写操作前提示登录（`code: 401`）。
- `schedule_notify`：定时触发，无用户上下文，不校验。

**验收**：
- 用 A 用户 token 调用接口，传 `user_id: B用户id`，操作对象仍是 A（数据按 A 隔离）。
- 无 token 时收藏/卡片/日程等写操作返回 401。

---

### Phase 2：数据库权限与索引（P0-3）

#### 2.1 schema 权限全量收紧

**问题**：`uni-id-users` 等 `read:true`，客户端 JQL 可读他人手机号/生日/收藏。

**原则**：客户端零直查，所有读写走云函数。**云函数内 DB 操作不受 schema permission 限制**，可放心全部收紧。

**改动文件**（除 `opendb-open-data` 外全部）：

| 集合 | `read` | `create` | `update` | `delete` |
|---|---|---|---|---|
| `uni-id-users` | `false` | `false` | `false` | `false` |
| `uni-id-token` | `false` | `false` | `false` | `false` |
| `daily_data` | `false` | `false` | `false` | `false` |
| `user_favorites` | `false` | `false` | `false` | `false` |
| `flashcards` | `false` | `false` | `false` | `false` |
| `groups` | `false` | `false` | `false` | `false` |
| `group_events` | `false` | `false` | `false` | `false` |
| `schedules` | `false` | `false` | `false` | `false` |
| `schedule_history` | `false` | `false` | `false` | `false` |
| `signups` | `false` | `false` | `false` | `false` |
| `signup_drafts` | `false` | `false` | `false` | `false` |
| `opendb-open-data` | 保持 `false` | `false` | `false` | `false` |

**同步前端**：删除/替换所有客户端直查，包括 `pages/search/search.vue:95-97` 的 `db.collection('user_favorites')`（改走云函数，见 Phase 3）。

**验收**：在小程序端尝试 `db.collection('user_favorites').get()` 应返回无权限错误；云函数内读写正常。

#### 2.2 索引清单（uniCloud 控制台「表结构」→「索引」逐条创建）

| 集合 | 索引字段（顺序） | 唯一 | 支撑查询 |
|---|---|---|---|
| `uni-id-users` | `openid` | ✅ | 登录查用户 |
| `uni-id-token` | `token` | ✅ | token 校验/清理 |
| `uni-id-token` | `user_id` | ❌ | 清理旧 token |
| `daily_data` | `date` | ❌ | 缓存命中 |
| `user_favorites` | `user_id` + `date` | ❌ | 收藏列表/日历打点 |
| `user_favorites` | `user_id` + `date` + `content` | ✅ | 收藏去重 |
| `flashcards` | `user_id` + `status` | ❌ | 列表/复习队列 |
| `groups` | `invite_code` | ✅ | 邀请码加入 |
| `groups` | `members.user_id` | ❌ | 我加入的群 |
| `group_events` | `group_id` + `type` + `createTime` | ❌ | 群动态流 |
| `schedules` | `owner_id` + `date` | ❌ | 个人日程 |
| `schedules` | `group_id` + `scope` + `date` | ❌ | 群日程 |
| `schedules` | `next_remind_at` | ❌ | **定时扫描（查询仅按此字段，勿建复合）** |
| `schedule_history` | `schedule_id` + `changeTime` | ❌ | 历史列表 |
| `signups` | `schedule_id` + `user_id` | ✅ | 报名去重/我的状态 |
| `signups` | `schedule_id` + `status` | ❌ | 名额统计/名单 |
| `signup_drafts` | `user_id` + `updateTime` | ❌ | 草稿列表 |

> 阿里云 uniCloud 不支持在 schema 文件中声明索引，需在控制台创建；唯一索引创建前请先清理历史重复数据（重复数据处理见 Phase 7 迁移脚本）。

**验收**：控制台「索引」列表与上表一致；热门查询执行计划不再全表扫描。

---

### Phase 3：核心业务缺陷修复（P0-4 / P0-5）

#### 3.1 `toggle_favorite.getMarks`：修复日程打点字段与语义

**问题**：`toggle_favorite/index.js:24-27` 用 `schedules.where({ user_id })`，字段应为 `owner_id`；且缺少"我报名的群日程"与"所在群群日程"。

**改动**（`toggle_favorite/index.js`）：

```js
if (action === 'getMarks') {
  const favs = await db.collection('user_favorites')
    .where({ user_id: uid }).field({ date: true }).get()

  // 我创建的日程（owner_id）
  const owned = await db.collection('schedules')
    .where({ owner_id: uid }).field({ date: true }).get()

  // 我报名的群日程：signups → schedules
  const signupRes = await db.collection('signups')
    .where({ user_id: uid, status: 'signed' }).field({ schedule_id: true }).get()
  const sids = (signupRes.data || []).map(s => s.schedule_id)
  let signed = []
  if (sids.length) {
    const sRes = await db.collection('schedules')
      .where({ _id: db.command.in(sids) }).field({ date: true }).get()
    signed = sRes.data || []
  }

  const marks = [
    ...(favs.data || []).map(i => ({ date: i.date, info: '已收藏' })),
    ...(owned.data || []).map(i => ({ date: i.date, info: '日程' })),
    ...signed.map(i => ({ date: i.date, info: '报名' }))
  ]
  return { code: 200, data: marks }
}
```

> 说明：`uid` 来自 Phase 1 的 `resolveUid`；`cmd.in` 上限约 100，群报名较多时分批查（或按月份查询，见 3.2 可选优化）。

**验收**：创建个人日程、报名群日程后，日历对应日期出现红点。

#### 3.2 日历打点月度化（P2，纳入 Phase 3）

**改动**：`getMarks` 增加必填 `month`（`YYYY-MM`），按 `date` 范围查询当月收藏与日程，避免全量打点：

```js
if (action === 'getMarks') {
  if (!month) return { code: 400, message: '缺少 month' }
  const [y, m] = month.split('-').map(Number)
  const start = month + '-01'
  const end = month + '-' + String(new Date(y, m, 0).getDate()).padStart(2, '0')
  const dateRange = db.command.gte(start).and(db.command.lte(end))
  const favs = await db.collection('user_favorites')
    .where({ user_id: uid, date: dateRange }).field({ date: true }).get()
  // ...同上（owned / signed），均加 date: dateRange
  return { code: 200, data: marks }
}
```

前端 `index.vue` 的 `loadCalendarMarks` 传入当月，`onMonthSwitch` 时重新拉取。

#### 3.3 `search.vue` 修复 + `pages.json` 注册

**问题**：`search.vue:60-63` 调用不存在的云函数 `uni-id-users`；且页面未注册进 `pages.json`。

**改动**：
1. `pages.json` 注册：
   ```json
   { "path": "pages/search/search", "style": { "navigationBarTitleText": "搜索" } }
   ```
2. `search.vue`：
   - `getUserId` 改为 `getUid()`（`utils/auth`）。
   - 卡片搜索：`callApi('flashcards', 'search', { keyword })`。
   - 收藏搜索：新增云函数 action（见 3.4），或合并为 `callApi('toggle_favorite', 'search', { keyword })`。
   - **删除**客户端 `db.collection('user_favorites')` 直查。

#### 3.4 `toggle_favorite` 增加 `search` action

```js
if (action === 'search') {
  if (!uid || !keyword) return { code: 200, data: [] }
  const res = await db.collection('user_favorites')
    .where({ user_id: uid }).limit(500).get()
  const kw = keyword.toLowerCase()
  const matched = (res.data || []).filter(i =>
    (i.content || '').toLowerCase().includes(kw) ||
    (i.author || '').toLowerCase().includes(kw)
  )
  return { code: 200, data: matched }
}
```

**验收**：搜索页能同时搜出卡片与收藏；游客无登录提示。

#### 3.5 `toggle_favorite` 增加 `checkBatch`（消除收藏状态 N+1）

**问题**：首页 `index.vue:338-356` 对每张卡片逐次调用 `check`，一次渲染 N 次云函数调用。

**改动**：

```js
if (action === 'checkBatch') {
  if (!uid || !date || !Array.isArray(contents)) return { code: 400, message: '缺少参数' }
  // contents: [{ content }]，批量匹配，返回 Map: content → isFavorited
  const res = await db.collection('user_favorites')
    .where({ user_id: uid, date, content: db.command.in(contents.map(c => c.content)) })
    .field({ content: true }).get()
  const favSet = new Set((res.data || []).map(i => i.content))
  return { code: 200, data: contents.map(c => ({ content: c.content, isFavorited: favSet.has(c.content) })) }
}
```

> 注意：`cmd.in` 一次约 100 项，卡片 >100 时前端分批调用（每次 100）。

**验收**：首页一次渲染只调用 1 次（或卡片>100 时 2 次）`checkBatch`，不再 N 次 `check`。

---

### Phase 4：权限边界与敏感信息（P1-6）

#### 4.1 `schedules.detail` / `history`：私有日程所有权校验

**问题**：`schedules/index.js:897-905` 只校验 group 日程，private 日程任何人可看；`history`（`schedules/index.js:640-648`）**完全无鉴权**，任何人凭 `schedule_id` 可读变更快照（含 before/after 全文）。

**改动**：

```js
if (action === 'detail' || action === 'history') {
  const res = await db.collection('schedules').doc(schedule_id).get()
  if (!res.data) return { code: 404, message: '日程不存在' }
  const item = res.data
  if (item.scope === 'private' && item.owner_id !== uid) {
    return { code: 403, message: '无权查看该日程' }
  }
  if (item.scope === 'group' && item.group_id && !(await isGroupMember(item.group_id, uid))) {
    return { code: 403, message: '非群成员不可查看' }
  }
  if (action === 'history') {
    const his = await db.collection('schedule_history')
      .where({ schedule_id }).orderBy('changeTime', 'desc').limit(50).get()
    return { code: 200, data: his.data || [] }
  }
  // ...原有 detail 逻辑
}
```

> 复用同一份权限判断，`detail` 与 `history` 同权。补充测试用例 T-410。

#### 4.2 `schedules.signups`：名单访问控制 + 敏感信息脱敏

**问题**：`schedules/index.js:858-894` 任何人可拉取报名名单（含手机号/生日）。

**改动**：
- 非群成员 → 403。
- 普通群成员：返回 `user_id/profile_name/group_remark/signed_at/form_data（不含手机号/生日）`。
- 群主 / 日程创建者：才返回 `profile_birthday`、`profile_phone` 与完整 `form_data`。

```js
if (action === 'signups') {
  // ... 查 schedule，校验存在
  if (schedule.scope !== 'group' || !schedule.group_id) return { code: 400, message: '该日程无报名' }
  const isMember = await isGroupMember(schedule.group_id, uid)
  if (!isMember) return { code: 403, message: '非群成员不可查看名单' }
  const isOwner = schedule.owner_id === uid || await isGroupOwner(schedule.group_id, uid)
  // 查询 signups 后：
  const data = (signupRes.data || []).map(s => {
    const base = { ...s, profile_name, group_remark }
    if (!isOwner) {
      base.profile_birthday = ''
      base.profile_phone = ''
      if (base.form_data && base.form_data.phone) base.form_data = { ...base.form_data, phone: '***' }
    }
    return base
  })
  return { code: 200, data }
}
```

#### 4.3 `flashcards`：所有权校验

**问题**：`updateStatus/delete` 只凭 `card_id` 可改删他人卡片。

**改动**：

```js
if (action === 'updateStatus') {
  const res = await db.collection('flashcards')
    .where({ _id: card_id, user_id: uid }).update({ status: new_status })
  if (res.updated === 0) return { code: 403, message: '卡片不存在或无权操作' }
  return { code: 200, message: '更新成功' }
}
// delete 同理，使用 where({ _id, user_id }).remove()
```

#### 4.4 `groups`：成员操作校验补全

- `detail`：已是群成员校验（有）。
- `setRemark`：增加 `me` 存在性校验（现有已做）。
- `events`：已有成员校验。

**验收**：用非本人账号访问他人私有日程 / 报名名单 / 卡片均返回 403。

---

### Phase 5：一致性 / 并发 / 提醒可靠性（P1-7/8/9）

#### 5.1 报名名额原子化 + 事务

**问题**：`schedules/index.js:701-715` count→add 竞态，高并发可超名额。

**方案（两层）**：
1. **schedules 增加 `signup_count` 字段**（默认 0），报名时原子递增并校验上限：
2. **报名整流程用事务**（`db.startTransaction()`）保证 场次名额 + 总名额 + 报名记录 + `signup_closed` 一起提交。

事务骨架：

```js
if (action === 'signup') {
  // 1. 前置校验（群成员/未关闭/未截止/表单校验）——事务外，快速失败
  // 2. 事务内核心操作
  const transaction = await db.startTransaction()
  try {
    const sRes = await transaction.collection('schedules').doc(schedule_id).get()
    if (!sRes.data || sRes.data.signup_closed) throw { code: 400, message: '报名已关闭' }
    const schedule = sRes.data

    // 总名额：signup_count 原子自增，超过 quota 则回滚
    if (schedule.signup_quota > 0) {
      const up = await transaction.collection('schedules')
        .where({ _id: schedule_id, signup_count: db.command.lt(schedule.signup_quota) })
        .update({ signup_count: db.command.inc(1) })
      if (up.updated === 0) throw { code: 400, message: '名额已满，无法报名' }
    }

    // 场次名额：事务内 count（快照隔离）
    if (clean.session && schedule.sessions && schedule.sessions.length) {
      const sItem = schedule.sessions.find(s => s.name === clean.session)
      if (sItem && sItem.quota > 0) {
        const cnt = await transaction.collection('signups')
          .where({ schedule_id, status: 'signed', 'form_data.session': clean.session }).count()
        if (cnt.total >= sItem.quota) throw { code: 400, message: '该场次名额已满' }
      }
    }

    // 写报名（唯一索引 (schedule_id,user_id) 兜底）
    await transaction.collection('signups')
      .where({ schedule_id, user_id: uid })
      .update({ status: 'signed', signed_at: Date.now(), cancelled_at: 0, form_data: clean })
    // 或 add（若更新 0 条）

    // 满员自动关闭
    if (schedule.signup_quota > 0) {
      const cnt = await transaction.collection('signups')
        .where({ schedule_id, status: 'signed' }).count()
      if (cnt.total >= schedule.signup_quota) {
        await transaction.collection('schedules').doc(schedule_id)
          .update({ signup_closed: true })
      }
    }
    await transaction.commit()
  } catch (e) {
    await transaction.rollback()
    return e.code ? { code: e.code, message: e.message } : { code: 500, message: '报名失败' }
  }
  // 事务外：logEvent + sendSignupNotify（可失败不影响主流程）
}
```

> 注意：阿里云 uniCloud 事务内需使用 `transaction.collection(...)` 对象，不能用 `db.collection`；`count` 在事务内支持。

**同步修改 `cancelSignup`**（`schedules/index.js:771-803`）——保持 `signup_count` 一致性：
- 取消报名时，若原记录为 `signed`，事务内 `signup_count: db.command.inc(-1)`（下限 0）。
- 若取消后 `signup_count < signup_quota`，同时 `signup_closed = false` 重新开放（替代当前"仅当 quota<=0 或 count<quota"的再查询）。
- `update/delete` 日程或解散群组删除 signups 时，`signup_count` 随文档删除自然清零（signups 随活动删除），无需单独处理。

**新增 schema 字段**：`schedules.schema.json` 增加 `signup_count: { bsonType: "int", description: "当前已报名人数（原子计数）" }`。

**兼容**：老数据 `signup_count` 缺失时按 0 处理（`cmd.exists` 兼容或迁移脚本补齐）；**迁移后 `attachSignupCounts`（`schedules/index.js:326`）可改为直接读 `signup_count` 字段，不再聚合查询**，从而消除重复聚合与 `cmd.in` 上限风险（配合 1.4 复核）。

#### 5.2 `toggle_favorite.add` 查重原子化

**改动**：依赖唯一索引 `(user_id, date, content)` + 捕获插入冲突：

```js
if (action === 'add') {
  try {
    await db.collection('user_favorites').add({ user_id: uid, date, content, author, source, createTime: Date.now() })
    return { code: 200, message: '收藏成功', isFavorited: true }
  } catch (e) {
    // 唯一索引冲突 = 已收藏
    return { code: 200, message: '已收藏', isFavorited: true }
  }
}
```

#### 5.3 `groups.join` 原子化

**问题**：`groups/index.js:62-70` 并发重复 push。

**改动**：

```js
// 先查后原子 push（用 where 条件兜底防重复）
await db.collection('groups').where({ _id: group._id, 'members.user_id': db.command.neq(user_id) })
  .update({ members: db.command.push({ user_id, role: 'member', profile_name: pname, group_remark, joinTime: Date.now() }) })
// 依据 update 返回 updated 判断是否真正加入
```

#### 5.4 `groups.setRemark` 并发安全

**问题**：整体替换 `members` 数组丢失并发修改。

**改动**：改为对目标成员做条件更新（利用数组内对象的唯一性做二次匹配），或引入事务。鉴于 uniCloud 数组元素更新能力有限，**推荐事务**：

```js
const transaction = await db.startTransaction()
const g = await transaction.collection('groups').doc(group_id).get()
// 校验成员/权限
const members = (g.data.members || []).map(m => m.user_id === target ? { ...m, group_remark } : m)
await transaction.collection('groups').doc(group_id).update({ members })
await transaction.commit()
```

#### 5.5 `schedule_notify`：发送成功才置 sent + 分批处理

**问题**：`schedule_notify/index.js:155-163` 先置 `sent=true` 再发送，失败即丢提醒；`limit(100)` 无排序。

**改动**：
1. 调整触发循环：**先收集到点提醒、逐条发送，全部成功后**再把 `reminders` 与 `next_remind_at` 写回；`sendMsg` 失败则该条不置 sent，下一轮重试（`reminders.sent` 保持 false）。
2. 拉取条件改为 `where({ next_remind_at: cmd.and(cmd.gt(0), cmd.lte(now)) })`，`orderBy('next_remind_at','asc')` + `limit(100)`；若本轮处理满 100 条仍有剩余，记录游标（`next_remind_at` 值）在本轮内**续拉**，直到处理完或达到单轮上限（如 500），避免超时。
3. **修正索引**：定时扫描只按 `next_remind_at` 过滤，2.2 中应建**单字段 `schedules.next_remind_at` 索引**（复合索引 `(date, next_remind_at)` 对 `next_remind_at ≤ now` 的查询无效）。
4. 发送成功后补写 `group_events`（去重逻辑保留）。
5. `backfillNextRemindAt`（`schedule_notify/index.js:112`）每轮执行 `next_remind_at: cmd.exists(false)` 全表扫描——改为**加 `backfill_done` 标记**（数据全量回填后置位，后续轮次跳过），或直接以 `next_remind_at` 单字段索引覆盖。

**补充**：`timeout` 由 10s 上调至 30s（`package.json` `cloudfunction-config`），配合单轮上限避免中途被杀。

#### 5.6 生日通知：派生字段 + 分批

**问题**：`schedule_notify/index.js:250` 正则全表扫描且 `limit(100)` 会漏。

**改动**：
1. `uni-id-users` 增加派生字段 `birthday_mmdd`（`MM-DD`），登录/报名沉淀生日时同步写入（`schedules/index.js:735-750` 与 `login.updateProfile` 处）。
2. `runBirthdayNotify` 改为 `where({ birthday_mmdd: todayMmdd, _id: cmd.not(...) })` 并分批（游标）处理，去掉正则与 `limit(100)` 截断。
3. 迁移脚本为存量用户回填 `birthday_mmdd`。

**验收**：
- 用 `console` 手动调用 `schedule_notify`（带 `TriggerName`）可正常触发。
- 模拟发送失败场景，确认该条下一轮重发且 `sent` 未被置位。

---

### Phase 6：配置治理与可维护性（P1-10、P2）

#### 6.1 密钥与模板 ID 集中管理（uni-config-center）

**改动**：
- 新增 `uni_modules/uni-config-center/uniCloud/cloudfunctions/common/uni-config-center/config.json` 或云函数环境变量，集中存放：

```json
{
  "tianapiKey": "***",
  "jinrishiciToken": "***",
  "wereadAppId": "wx8a5d6f9fad07544e",
  "singlecalAppId": "wxf510f247ff69b85e",
  "dcloudAppid": "__UNI__87DED5E",
  "templates": {
    "scheduleNotify": "UVDiKNJ5K6pYWCC94empCnhmVjMep3nfOmkgQmYd2J0",
    "birthdayNotify": "y-wxULiiW1d6CBRYU7EJqs2KGSAzCbJ5sqA3p2culuc",
    "signupPublish": "46MLVHkksG2sXsydt_CuGDdRlW1Pdhv5YjdHf1Ojpgc",
    "signupResult": "UVDiKNJ5K6pYWCC94empCnhmVjMep3nfOmkgQmYd2J0"
  }
}
```

- 云函数内 `require('uni-config-center')` 读取；`get_daily_quote` 移除源码中的 `TIANAPI_KEY`（`get_daily_quote/index.js:1`），`schedules`/`schedule_notify` 移除硬编码模板 ID 与 `DCLOUD_APPID`。
- 前端 `index.vue` 中的 `JINRISHICI_TOKEN`/AppID 保留（小程序端必须），但建议把今日诗词改为**经云函数代理**（`get_daily_quote` 增加 `jinrishici` 来源），从而前端不再携带 token。
- `uni-id.config.json` 的 `tokenSecret`/`passwordSecret` 换成强随机串并移出仓库（.gitignore）。

**验收**：`grep -r "TIANAPI_KEY\|appsecret\|JINRISHICI_TOKEN" uniCloud-aliyun/cloudfunctions` 仅剩配置中心引用。

#### 6.2 `schedules` 拆分（单入口 + 子域文件）

**改动**：保持 `schedules/index.js` 作为入口路由，将逻辑抽到子模块：

```
schedules/
  index.js          // action 路由 + resolveUid
  lib/
    auth.js         // resolveUid（可从 common 模块复用）
    schedule.js     // create/update/delete/getMonth/getDay/search/getGroupSchedules/detail/history/getBirthdays
    signup.js       // signup/cancelSignup/updateActivity/signups/publishSignup
    draft.js        // saveSignupDraft/listSignupDrafts/deleteSignupDraft
    notify.js       // subscribeNotify/getNotifyState/sendSubMsg/sendPublishNotify/sendSignupNotify
    util.js         // computeNextRemindAt/logHistory/logEvent/cleanSignupForm/isGroupMember 等
```

> 长期若 action 继续膨胀，可将 `signup.js` 独立为云函数 `signup`，前端调用名同步修改。

#### 6.3 级联清理补全

- `groups.disband`（`groups/index.js:188-192`）追加：删除 `schedule_history`（该群日程的）、`signup_drafts`（group_id 匹配）；并清理 `uni-id-users.group_ids`（若保留该字段）。
- 决策：**废弃 `uni-id-users.group_ids` 字段**（从未维护，`groups.members` 是唯一事实源），从 schema 中删除字段定义；迁移脚本清空存量值。
- `schedules.delete`：`schedule_history` 保留作为审计（建议只增不删），`group_events` 的 `schedule_deleted` 已有；`signups` 已删除。

#### 6.4 `daily_data` 缓存策略

- `get_daily_quote` upsert 时：删除同日期其他 `api_cache` 文档（防多缓存孤儿）。
- 增加 `expireAt`（当日缓存 `date + 1天` 过期）；历史日期缓存不清（时光机语义），当日内容到点后自动回源刷新。
- **并发单飞**：由于 `daily_data.date` 已有非唯一索引，缓存未命中时多个并发请求可能同时回源抓取外部 API。为控制抓取次数，可用「单飞锁」：先 `add` 一条 `pending` 状态的缓存记录（`date` 唯一约束或 `where({date,status:'pending'}).update` 抢占），抢到才回源，其余请求等待/读旧缓存（详见 6.7 复用同一机制）。
- 前端 `loadData` 的兜底文案与云函数空结果兜底二选一（保留前端 FALLBACK_QUOTES，删除云函数端"暂无数据"占位，减少歧义）。

#### 6.5 `flashcards` 批量导入

- 新增 `batchCreate` action：一次 `add` 多条（`db.collection('flashcards').add(arr)`），前端批量导入逐条调用改为单次批量。

#### 6.6 `schedule_notify` 模板统一

- 统一使用 `uni-config-center` 的 templates；`TEMPLATE_ID` 与 `SIGNUP_RESULT_TMPL` 同 ID 问题通过配置中心单一来源消除。
- `sendSubMsg` 的 `page` 参数按场景传入（日程详情 `pages/schedule-detail/schedule-detail?schedule_id=xxx`），不再固定 `pages/schedule/schedule`。

**验收**：各云函数通过 HBuilderX「上传并运行」无硬编码密钥告警；`schedules` 仍可正常完成全部原 action。

#### 6.7 今日诗词改云函数代理 + 按日缓存

**问题**：`index.vue:235-258` 前端直调 `https://v2.jinrishici.com/sentence`，每次进首页/切日期都重新请求（**无缓存、重复抓取**），且 `JINRISHICI_TOKEN` 随小程序包分发。

**改动**：
1. `get_daily_quote` 增加 `jinrishici` 来源：命中 `daily_data` 当日缓存则直接返回；未命中时由云函数调 jinrishici，与其余来源合并后写入 `api_cache`。
2. `index.vue`：
   - 删除 `fetchJinrishici`（`index.vue:235-258`）与 `JINRISHICI_TOKEN`（`index.vue:155`）。
   - `loadData` 只调 `get_daily_quote`，`Promise.all` 合并到单次调用。
   - `source='jinrishici'` 卡片的数据由云函数返回，前端 `sourceLabel` 逻辑保留。
3. 更新《开发方案思考.md》：今日诗词由「前端直调」改为「云函数代理」。
4. 并发单飞：见 6.4，用 `pending` 抢占防止并发回源。

**验收**：连续进入首页/切换日期，`get_daily_quote` 当日只回源一次；`grep -r "jinrishici\|JINRISHICI" calendar` 仅剩云函数引用；开发空间日志确认无前端直调。

---

### Phase 7：存量数据迁移（token → uid）

> 目的：修复历史上写入的 `user_id`/`owner_id` 为 token 哈希的数据，映射到真实 uid。

**识别规则**：Mongo `_id` 为 24 位十六进制；SHA256 为 64 位十六进制；游客为 `visitor_` 前缀。因此：**长度为 64 的十六进制字符串即为 token 哈希**，需要替换。

**步骤**：

1. 从 `uni-id-token` 建立映射：`token_hash → uid`（`user_id` 字段）。
2. 对以下集合按字段批量替换：

| 集合 | 字段 |
|---|---|
| `user_favorites` | `user_id` |
| `flashcards` | `user_id` |
| `schedules` | `owner_id` |
| `signups` | `user_id` |
| `signup_drafts` | `user_id` |
| `schedule_history` | `changed_by` |
| `groups` | `creator_id`、`members[].user_id` |
| `group_events` | `user_id` |
| `uni-id-token` | `user_id`（本已正确，跳过） |

3. **去重**：替换后同一用户可能出现重复记录。按唯一索引目标去重：
   - `user_favorites`：保留每组 `(uid, date, content)` 最早一条。
   - `signups`：保留每组 `(schedule_id, user_id)` 最新一条。
   - `groups.members`：按 `user_id` 去重（保留 role=owner 优先）。
   - `flashcards`/`schedules`/`group_events`/`schedule_history`：直接替换即可，通常不产生重复。
4. **回填派生字段**：`uni-id-users.birthday_mmdd`、`schedules.signup_count`（按 signups 统计）。
5. **清理**：`uni-id-users.group_ids` 字段置空或删除；`uni-id-token` 每用户只保留最新一条。

**实施载体**：编写一次性云函数 `migrate_user_id`（控制台手动调用，`event.dryRun=true` 先预览替换数量，确认后 `dryRun=false` 执行），**禁止在生产客户端调用**。执行前先做 Phase 0 备份。

**回滚**：若迁移后数据异常，用 Phase 0 备份整库恢复。

**验收**：
- `dryRun` 输出各表将替换条数、去重条数。
- 执行后 `grep` 确认不再存在 64 位十六进制 `user_id`（除 `uni-id-token.token` 外）。
- 登录同一账号可见历史收藏/卡片/日程。

---

### Phase 8：回归验证与上线

#### 8.1 功能回归清单

| 模块 | 用例 | 通过标准 |
|---|---|---|
| 登录 | 新用户/老用户/游客 | 返回 uid+token，重启后 uid 不变 |
| 鉴权 | 伪造 token / 过期 token / 他人 uid | 401 / 403 |
| 每日一言 | 缓存命中/回源/切换日期 | 当日内容正确，历史日期可看 |
| 收藏 | 添加/取消/去重/日历打点 | 打点含收藏+个人日程+报名 |
| 卡片 | 单条/批量/搜索/状态切换/删改 | 仅本人可见可改 |
| 群组 | 建群/邀请码加入/备注/退出/解散 | 级联清理完整 |
| 日程 | 创建/编辑/删除/详情/权限 | 私有日程他人 403 |
| 报名 | 报名/取消/名额满/场次满/并发 | 并发不超卖，名单脱敏 |
| 提醒 | 日程提醒/生日提醒/失败重试 | sent 仅成功后置位 |
| 搜索 | 卡片+收藏 | 走云函数，游客可提示登录 |

#### 8.2 并发/压力冒烟

- 用脚本并发 200 次报名同一活动（quota=50）→ 报名成功数 = 50。
- 并发 50 次收藏同一内容 → 唯一记录 1 条。

#### 8.3 上线顺序

1. Phase 0 备份 → 2. Phase 7 迁移（先 dryRun）→ 3. Phase 1~6 代码随版本发布（schema 权限收紧需与"前端移除直查"同版本上线，避免功能中断）→ 4. 控制台建索引 → 5. 回归验收。

#### 8.4 风险与回滚

| 风险 | 缓解 |
|---|---|
| schema 收紧导致旧版本客户端无法使用 | 权限收紧与前端发布同版本；保留 opendb-open-data 不动 |
| 迁移误替换 | dryRun 预览 + 整库备份 + 分表分批执行 |
| 事务性能 | 仅核心写路径（报名/备注/解散）用事务，读路径不变 |
| uni-id-common 版本兼容 | 锁版本，先单独上线 `login` 验证 |

---

## 四、附录

### 4.1 涉及文件总表

| 文件 | 阶段 |
|---|---|
| `utils/auth.js`、`utils/cloud.js`（新增） | 1 |
| 12 个页面（getUserId/callFunction 替换） | 1/3 |
| `cloudfunctions/login/index.js` + `package.json` | 1 |
| `cloudfunctions/common/uni-id-common`（复用 uni_modules） | 1 |
| 6 个业务云函数 `index.js`（入口鉴权 + 对应修复） | 1/3/4/5 |
| 11 个 `database/*.schema.json`（权限收紧 + 新增字段） | 2/5 |
| `pages.json`（注册 search） | 3 |
| `pages/search/search.vue` | 3 |
| `uni_modules/uni-config-center`（新增配置） | 6 |
| `uni-id.config.json` | 6 |
| 一次性 `migrate_user_id` 云函数（新增） | 7 |

### 4.2 新增/废弃字段汇总

| 集合 | 新增字段 | 废弃 |
|---|---|---|
| `schedules` | `signup_count` (int) | — |
| `uni-id-users` | `birthday_mmdd` (string) | `group_ids` |
| `daily_data` | `expireAt` (timestamp) | — |

### 4.3 需在控制台确认的配置

- 云函数环境变量：`TIANAPI_KEY`、`JINRISHICI_TOKEN`、微信 `appid/appsecret`、订阅模板 ID。
- 索引 16 条（见 2.2）。
- 定时触发器：`schedule_notify`（日程 cron + 生日 cron，确认已配置）。

---

> 本方案为整改蓝本，实施时建议按 Phase 逐项验收、独立提交；涉及代码的改动请在本方案确认后另行逐项落地。
