# 云函数与数据库整改方案 · 可行性综合评估报告

> 版本：v1.0 ｜ 评估日期：2026-08-16
> 评估对象：《云函数与数据库设计分析.md》《云函数与数据库整改方案.md》
> 对照依据：《整改方案检查报告.md》《整改方案测试方案与用例.md》及仓库实际代码
> 方法：逐条读取四份文档并将结论与实际代码（`cloudfunctions/*`、`database/*.schema.json`、`pages/*`）逐一交叉验证，然后对整改方案的每一项改动做「可行性 / 风险 / 前置条件」三维评估。

---

## 一、评估结论速览

| 维度 | 结论 |
|---|---|
| 分析文档准确性 | ✅ 高。逐条核对了 4 个 P0、5 个 P1 的代表性结论，均与实际代码吻合（细节见下文 §二） |
| 整改方案总体可行性 | ✅ 可行。方向正确、分阶段合理、架构决策（uid 统一身份、uni-id-common、schema 全封闭、事务化、配置中心）均可在现有代码与已内置 uni_modules 基础上落地 |
| 基建就绪度 | ✅ 良好。`uni_modules` 已内置 `uni-id-common`、`uni-config-center`、`uni-open-bridge-common`、`uni-subscribemsg`，`schedules` 已示范 `file:../common/...` 的依赖引用模式 |
| 需修正/补强的点 | ⚠️ 6 处（见 §三：G-C1~G-C6，与既有检查报告 G-01~G-06 互补，含 1 处方案新增风险与 1 处现有检查报告未覆盖点） |
| 需业务决策项 | 2 处（见 §四：D-C1/D-C2） |
| 建议 | 方案可执行，但应按本报告补强后再进入实施与测试阶段 |

---

## 二、分析文档准确性抽查（代码交叉验证）

以下为本次对实际仓库代码的逐一核验结果，作为评估整改方案可行性的前提（分析不准确之处会直接影响整改方向）。

### 2.1 已核验为「属实」的关键结论

| 分析声称 | 实际代码坐标 | 核验结果 |
|---|---|---|
| P0-1 前端 `getUserId()` 优先返回 `uni_id_token` | `pages/{index,favorites,schedule,schedule-detail,signup-create,...}.vue` 及编译产物 `.js` 中 `getUserId = () => { const token=getStorageSync('uni_id_token'); if(token) return token; ... }` | ✅ 属实。token 优先于 uid，且 `login/index.js` 每次登录 `generateToken(userId)` 生成新哈希并 `add` 进 `uni-id-token` → 重登后 user_id 变化 |
| P0-2 `getCurrentUser`/`phoneLogin` 把 token 当 `_id` | `login/index.js`: `db.collection('uni-id-users').doc(token).get()` 与 `.doc(event.token).update({phone})` | ✅ 属实。`uni-id-users` 的 `_id` 是 userId，非 SHA256 token → 必然 404/写入失败 |
| P0-3 schema 全开 | `uni-id-users.schema.json` read:true、`user_favorites.schema.json` 全 true、`schedules/signups` read:true | ✅ 属实。`search.vue` 直接 `db.collection('user_favorites').where(...).get()` 可读他人收藏 |
| P0-4 `getMarks` 用错字段 | `toggle_favorite/index.js`：`schedules.where({ user_id })`，而 `schedules` 创建者字段为 `owner_id` | ✅ 属实。日程打点恒空 |
| P0-5 搜索页调用不存在云函数 + 未注册 | `search.vue` 调 `name:'uni-id-users'`（无此云函数）；`pages.json` 无 `pages/search/search` | ✅ 属实。文件 `search/` 存在但未注册 |
| P1-6 私有日程/报名名单越权 | `schedules/index.js` `detail` 仅在 `scope==='group'` 时校验成员；`signups` 无成员校验且拼装 `profile_birthday/profile_phone` | ✅ 属实。`history` 同样仅按 schedule_id 读取 |
| P1-7 报名名额非原子 | `schedules/index.js` 报名流程先 `count` 后 `add`，无事务/条件更新 | ✅ 属实 |
| P1-9 发送失败仍置 sent | `schedule_notify/index.js` 发送循环先标记 `sent=true` 再 `sendMsg`，失败不回滚 | ✅ 属实 |
| P1-10 密钥/AppSecret 硬编码 | `get_daily_quote/index.js:1` 明文 `TIANAPI_KEY`；`login/config.json` 存 wx appsecret；`uni-id.config.json` 明文 appsecret | ✅ 属实（`login/config.json` 因含密钥被工具拒绝读取，反向印证硬编码） |
| 基建可复用 | `uni_modules` 内含 `uni-id-common`、`uni-config-center`、`uni-open-bridge-common`、`uni-subscribemsg`；`schedules/package.json` 已用 `file:../common/uni-subscribemsg` | ✅ 属实。为整改方案依赖声明提供了现成范式 |

### 2.2 既有检查报告的正确性确认

本次撰写的《整改方案检查报告》所提缺口（G-01 唯一索引时机、G-04 取消报名不计减 `signup_count`、G-05 按条标记、G-06 `cmd.in` 分批、R-01 content_hash）经代码复核均成立，本报告不再重复，仅就既有报告**未覆盖或需补强**的点做增量补充（§三）。

---

## 三、整改方案条目级可行性评估（含补强项）

> 说明：每条给出「可行性 P=n / n」与是否需要补强。既有检查报告已闭环的条目以简评带过，重点标注需修正处。

### Phase 0 数据快照与基线 —— ✅ 可行性 4/4
- 评估：只读导出，无技术风险，纯操作流程。
- 补强：建议使用 uniCloud 控制台「导出」时**同时校验导出数量与线上 count 一致**，并导出 `JQL查询.jql` 中已使用的工具脚本；git tag 前确认工作区无未提交改动。

### Phase 1 身份与鉴权（P0-1/P0-2）—— ✅ 可行性 4/4，⚠️ 需注意 1 点
1. **1.1 前端 `utils/auth.js` / `utils/cloud.js`**：可行。注意既有报告 G-02 的「游客/半登录空 uid 边界」，需让 `getUid()` 恒返回非空字符串。
2. **1.2 login 接入 uni-id-common**：可行性高。`uni-id-common` 已内置；`schedules/package.json` 已验证 `file:../common/...` 依赖方式可用。
   - ✅ 补充验证：`uni-id.config.json` 已含 `tokenExpiresIn:2592000`（30 天）与 `passwordSecret/tokenSecret` 字段，`uniID.createToken/checkToken` 可直接读取配置，无需额外改配置结构。
   - ⚠️ **补强（G-C1）**：现有 `uni-id.config.json` 的 `tokenSecret`/`passwordSecret` 为弱口令明文（`"salt-words-are-not-important"` 等）且 **`applets.wx.oauth.weixin.appsecret` 硬编码**。接入 uni-id-common 后这些密钥将实际生效并被用于 JWT 签名 / wx 会话，风险等级从「未启用」升级为「真实被利用」。必须在启用 uni-id-common **之前或同版本**完成密钥轮换（强随机化 + 移出仓库，纳入 `.gitignore` 与配置中心），否则等于把现网密钥直接启用。
   - **补强（G-C2，方案新增风险点）**：`phoneLogin` 当前用 `event.token` 当 `_id`；方案改为「checkToken → 按 uid 绑定」。但需注意 `updateProfile` 目前是 `event.uid || event.user_id` 信任客户端，方案 1.2 已改从 token 解析 uid，方向正确；需在实施时确保**所有写档案路径统一走 uni-id-common 校验**，避免遗留一条可直改他人档案的旁路。
3. **1.3 各云函数入口 `resolveUid`**：可行。7 个云函数接入清单合理（login 重构、get_daily_quote/schedule_notify 不校验、其余 resolveUid）。
   - ⚠️ **补强（G-C3，既有报告未覆盖）**：`get_daily_quote` 虽公开，但 Phase 2 将 `daily_data` 权限收紧为 false 后，云函数内读写不受 schema 限制，**无需改动**；但若保留客户端写/删 `daily_data` 的任何入口会失效——需确认前端不存在 `daily_data` 直写。经查前端仅经云函数读，安全。

### Phase 2 数据库权限与索引（P0-3）—— ✅ 可行性 4/4，⚠️ 1 处顺序需调整
- **2.1 schema 全收紧**：可行，且已验证云函数内 DB 操作不受 schema permission 限制的判断成立（云函数走服务端权限，`uniCloud.database()` 在云函数内默认具备完整权限）。
- ⚠️ **补强（对齐既有 G-01）**：唯一索引（`uni-id-token.token`、`user_favorites(user_id,date,content)`、`signups(schedule_id,user_id)`、`groups.invite_code`、`uni-id-users.openid`）**必须先做数据去重→再建**，否则存量重复数据导致建索引失败。建议把唯一索引创建全部移到 Phase 7 迁移完成后；Phase 2 只建非唯一索引。
- **2.2 索引清单**：可行且必要。补充两点：
  - `uni-id-token.token` 唯一索引支撑 `resolveUid` 反查——但见 §四 D-C1（JWT 无状态校验是否真正查表）。
  - 既有建议 R-01 的 `content_hash`：`user_favorites` 收藏 news/历史长文时 `content` 可能超 MongoDB 复合索引 key 长度限制，**强烈建议纳入本次**而非仅作低优先级备注。

### Phase 3 核心业务缺陷（P0-4/P0-5）—— ✅ 可行性 4/4
- **3.1 `getMarks` 修字段 + 增报名打点**：可行。注意 `cmd.in(sids)` 上限（既有 G-06 一致性）：报名集合第 101 条后需分批。建议直接给 `getMarks` 增加 `month` 参数按当月 `date >= start and <= end` 查询（3.2），同时规避 in 上限与全量日期问题。
- **3.3/3.4 search 修复 + 新增 `toggle_favorite.search`**：可行，验证了 `search.vue` 现状（调 `uni-id-users` + 客户端直查 `user_favorites`），方案删除直查、改走云函数正确。`pages.json` 需新增注册条目。

### Phase 4 权限边界与敏感信息（P1-6）—— ✅ 可行性 4/4，⚠️ 需补 history
- **4.1/4.2/4.3/4.4**：detail/signups/flashcards/groups 改动合理。
- ⚠️ **补强（对齐既有 G-03）**：方案漏了 `schedules.history`（`schedules/index.js` 仅凭 `schedule_id` 读变更历史，含 before/after 全量快照）。必须复用 `detail` 的权限判断（private=owner、group=群成员）。这是 P1-6 同类越权，建议**升级为必须修**。

### Phase 5 一致性/并发/提醒可靠性（P1-7/8/9）—— ✅ 可行性 3.5/4，⚠️ 3 处需修正
- **5.1 报名事务**：方案事务骨架正确（`transaction.collection`、`cmd.inc(1)` 条件更新、满员关报名）。
  - ⚠️ **补强（对齐既有 G-04，评估为必须修）**：`signup_count` 必须与 `cancelSignup` `updateActivity`/`delete` 联动（取消 -1、低于 quota 重新开放），否则计数漂移导致「占住名额」错乱。既有 T-503 需补 `signup_count` 断言。
  - ⚠️ **补强（新增）**：事务的「满员自动关闭」与「取消重新开放」需共用同一套阈值判断，建议将「重算 signup_count 与 signup_closed」抽为公共函数 `recomputeSignupState(schedule_id)` 在事务内调用，避免报名/取消两处逻辑漂移。
- **5.2 收藏唯一索引 + catch 冲突**：可行。但见 §R-01 的 `content_hash` 前提。
- **5.3 join 条件 push / 5.4 setRemark 事务**：可行。
- **5.5 schedule_notify 按条置 sent + 分批**：
  - ⚠️ **补强（对齐既有 G-05）**：明确「每条独立判定、成功才置 sent」，废掉「全部成功后写回」表述。
  - ⚠️ **新增建议（对齐既有 R-06）**：定时触发器需防 cron 重叠（双实例并发双发）；建议加运行锁字段或 `uni-id` 单实例受限运行。
- **5.6 生日派生字段 `birthday_mmdd`**：可行，消除正则全表扫描。回填脚本纳入 Phase 7。

### Phase 6 配置治理与可维护性（P1-10、P2）—— ✅ 可行性 4/4，⚠️ 1 处冲突
- **6.1 密钥集中管理**：可行。`uni-config-center` 已内置。
  - ⚠️ **补强（D-C2 反向确认）**：今日诗词改云函数代理会改变既有「前端直调 + Token」架构（`index.vue` 的 `Promise.all` 并行 + token 随包分发）。需业务决策：接受架构变更（前端不再带 token），或保持直调并明确接受 token 暴露（今日诗词 token 本身公开可申请，风险低）。若保持直调，`get_daily_quote` 无需加 `jinrishici` 来源。
- **6.2 schedules 拆分 / 6.3 级联清理 / 6.4 缓存策略 / 6.5 batchCreate / 6.6 模板统一**：均可行。级联清理中「废弃 `uni-id-users.group_ids`」决策合理（经查该字段从未被 groups 写入，冗余无实际作用）。
  - ⚠️ **补强（新增，低成本高收益）**：`attachSignupCounts`（schedules）目前用 `cmd.in(ids)` 一次性查最多 500 条，超过 uniCloud `in` 上限即失败。除分批外，**优先建议改用 `signups` 聚合**（`aggregate().match({schedule_id:in(ids)}).group({_id:'$schedule_id',n:sum(1)})`）一次取回全部计数，避免多次往返。这与既有 G-06 一致。
  - ⚠️ **补强（新增 G-C4）**：`groups.invite_code` 建唯一索引后，需在 `groups.create` 捕获唯一冲突并**重新生成邀请码重试**（否则碰撞由「低概率」升级为「必然抛错」）。既有 R-02 已提到，建议**纳入本次**而非遗留。

### Phase 7 存量数据迁移（token→uid）—— ✅ 可行性 3.5/4，⚠️ 依赖前置
- **可行性**：迁移逻辑（64 位 hex 识别 token 哈希 + `uni-id-token` 映射 + 逐表替换 + 去重 + 回填派生字段 + dryRun）设计完整、可回滚（Phase 0 备份）。
- ⚠️ **前置依赖**：①必须**先清理每用户多余 token**（方案 1.2 已含登录清旧 token，但存量仍需迁移脚本处理）；②**唯一索引必须在本阶段之后创建**（G-01）；③`dryRun` 需输出「逐表替换/去重/将受影响 userId」三份预览，人工核对后再执行。
- ⚠️ **补强（G-C5，方案测试未充分覆盖）**：`groups.members[]` 内嵌数组按 uid 去重时，若同一用户既做过 owner 又做过 member（历史数据异常），需定义保留优先级（owner 优先）。测试用例 T-705 建议补一条「同一 uid 在 members 多次出现」的断言。

### Phase 8 回归验证与上线 —— ✅ 可行性 4/4
- 回归清单/并发冒烟/上线顺序/风险回滚完整。补充：
- ⚠️ **补强（G-C6，上线窗口提示）**：schema 从全开收紧为 false 的版本，必须与「移除前端全部客户端直查」**同一版本发布**，否则旧包在新 schema 下直查全部报错（既有方案已提，此处强调为 Blocker 级前提）。上线前请全仓 grep `collection(` 在前端 `.vue` 中的直查残留。

---

## 四、需业务决策确认项

| 编号 | 决策 | 选项 | 建议 |
|---|---|---|---|
| D-C1 | JWT 删除 DB 记录 ≠ 真正吊销（uni-id-common `checkToken` 为无状态签名校验，不查 `uni-id-token` 表） | A) 接受：微信小程序用户量有限 + JWT 30 天过期，可容忍泄露窗口；B) `resolveUid` 校验签名后再反查 `uni-id-token` 是否存在该 token，牺牲性能换可吊销 | 默认 A；如有封号/踢人需求再升 B |
| D-C2 | 今日诗词是否改云函数代理 | A) 改代理：前端不再携 token，但需调整 `index.vue` 并行拉取逻辑与架构文档；B) 保持前端直调：接受包内 token（该 token 公开可申请，风险低） | 倾向 A（最终统一密钥治理）；若求快可 B 并记录风险 |

---

## 五、改进措施明细表（可直接执行）

> 优先级：P0（必须，阻断上线）/ P1（应尽快）/ P2（优化）。列「来源」标明是既有检查报告（R）/新增（C）。

| # | 优先级 | 措施 | 落点 | 来源 |
|---|---|---|---|---|
| M-1 | P0 | login 接入 uni-id-common 的同时轮换 `tokenSecret/passwordSecret/wx.appsecret` 为强随机并移出仓库（.gitignore + 配置中心），避免「启用即把弱密钥暴露给真实签名」 | login、uni-id.config.json、uni-config-center | C1 |
| M-2 | P0 | 唯一索引创建统一推迟到 Phase 7 迁移去重完成后执行；Phase 2 仅建非唯一索引 | 控制台索引、迁移脚本 | R/G-01 |
| M-3 | P0 | `schedules.history` 复用 `detail` 权限校验（private=owner、group=群成员） | schedules lib/schedule.js | R/G-03 |
| M-4 | P0 | `signup_count` 与 cancelSignup/updateActivity/delete 联动递减并按阈值重算 `signup_closed`；抽公共 `recomputeSignupState` | schedules lib/signup.js | R/G-04 + C |
| M-5 | P0 | schema 收紧版本与「移除前端全部 `db.collection()` 直查」同版本上线；上线前全仓 grep 直查残留 | schema + 所有 pages/*.vue | C6 |
| M-6 | P1 | `schedule_notify` 明确「每条独立」判定成功才置 sent；失败保持原状待重发 | schedule_notify | R/G-05 |
| M-7 | P1 | `attachSignupCounts` 改 `signups` 聚合（match+group）一次性取计数，消除 `cmd.in` 超限；`getMarks` 加 month 参数按当月查询 | schedules、toggle_favorite | R/G-06 + C |
| M-8 | P1 | `user_favorites` 唯一索引改用 `content_hash`（SHA256）字段，规避长文本 key 超限 | schema + toggle_favorite | R/R-01 |
| M-9 | P1 | `groups.create` 捕获 invite_code 唯一冲突并改码重试 | groups | R/R-02 + C4 |
| M-10 | P1 | `utils/auth.js` 保证 `getUid()` 恒返回非空字符串，明确「已登录/游客/未初始化」三层身份 | utils/auth.js | R/G-02 |
| M-11 | P1 | `schedule_notify` 增加运行锁/实例互斥，防 cron 重叠双发 | schedule_notify | R/R-06 |
| M-12 | P2 | `toggle_favorite` 增加 `checkBatch`，消除首页逐卡 N+1 | toggle_favorite | R/R-03 |
| M-13 | P2 | 云函数鉴权抽公共模块（`common/uni-verify`），消除多处重复 `resolveUid`；需新建 `cloudfunctions/common/` 目录 | 新建 common | R/R-04 |
| M-14 | P2 | `groups.members` 去重定义 owner 优先优先级；T-705 补同 uid 多次出现用例 | 迁移脚本 + 测试 | C5 |
| M-15 | P2 | 确认 D-C2 后决定今日诗词走代理或保持直调，并同步架构文档 | get_daily_quote、index.vue | D2 |

---

## 六、最终结论

1. **分析文档可信**：本轮将 4 个 P0、5 个 P1 的代表性结论与实际代码逐一比照，全部属实，可作为整改依据。
2. **整改方案可行**：方向正确、分阶段可独立上线、回滚方案完备，且基建（uni-id-common / uni-config-center / uni-subscribemsg）已在 `uni_modules` 就绪，`file:../common/...` 依赖范式已被 `schedules` 验证。
3. **必须先处理的风险**：接入 uni-id-common 会**立即激活当前仓库里的弱密钥/明文 appsecret**（M-1，P0）；schema 收紧必须与前端去直查同版本（M-5，P0）。这两条若忽略，会让「整改」本身引入比现状更严重的安全风险。
4. **建议执行路径**：按「M-1/M-5 前置安全加固 → M-2~M-10 核心整改 → M-11~M-15 优化」，逐项以既有测试方案为验收基线，并在开发空间完成事务/并发用例（T-501~T-506）实测后再上生产。

> 本报告为独立可行性评估，未对任何代码或文档做修改；实施时建议将 M-1~M-15 合并进《整改方案》对应 Phase 逐项落地。
