# 整改执行蓝本（唯一施工源）

> v1.0 ｜ 2026-08-16
> 本文件为**唯一施工依据**，整合此前 8 份文档的所有结论。实施以本文件为准，每步标注「📍可编码」或「🎛控制台操作」。
> 来源文档：
> - 《云函数与数据库设计分析》→ P0/P1/P2
> - 《云函数与数据库整改方案》→ 8 个 Phase
> - 《整改方案检查报告》→ G-01~G-06、R-01~R-06
> - 《整改方案可行性综合评估报告》→ M-1~M-15
> - 《云函数调用·定时·缓存专项评估报告》→ R1~R6、T1~T5、C1~C5

---

## 一、为什么需要重新梳理

原《整改方案》结构完整，但被后续 3 份报告发现了**若干未并入方案的修正项**，若直接照原方案动手会遗漏：

| 后续发现 | 归口 | 原方案是否已含 |
|---|---|---|
| T3 生日定时器时区错位（定时器实际不触发） | Phase 5 | ❌ 未含 |
| C2 `forceRefresh` 语义混乱（回源却标 cached） | Phase 6.4 | ❌ 未含 |
| C1 `daily_data` 无 TTL（当日缓存永不过期） | Phase 6.4 | ⚠️ 仅提 expireAt 无实现细节 |
| R1 收藏 N+1（`checkFavoriteStatus` 逐卡 check） | Phase 3 | ❌ 未含 |
| G-06 / M-7 `attachSignupCounts` 的 `cmd.in` 超限 | Phase 3.2 | ⚠️ 仅 getMarks 提示，未波及 schedules |
| M-1 弱密钥「启用即暴露」的前置安全加固 | Phase 1/6 | ⚠️ 提及但未列为阻塞 |
| M-5 schema 收紧必须与前端去直查同版本 | Phase 8.3 | ✅ 有，强化为 Block |
| R3 getMarks/getMonth 全量拉取 | Phase 3.2 | ⚠️ 仅建议未落地 |
| M-8 收藏唯一索引用 `content_hash`（长文超限） | Phase 2.2 | ❌ 用 content 而非 hash |
| G-03 `schedules.history` 越权 | Phase 4 | ❌ 未含 history |
| G-04 signup_count 取消报名不联动 | Phase 5.1 | ❌ 未含 |

本蓝本将上述全部并入对应 Phase，并区分「可编码」与「控制台操作」。

---

## 二、可编码改动清单（本次落地）

本次从**纯代码层面**落地以下文件（不依赖 uniCloud 控制台即可编写）：

### A. 新增前端工具
- `calendar/utils/auth.js`
- `calendar/utils/cloud.js`

### B. 前端页面调用迁移（10+2 页）
- `pages/index`、`favorites`、`anki`、`create-card`、`schedule`、`create-schedule`、`schedule-detail`、`groups`、`group-detail`、`signup-create`、`search`、`login`

### C. 云函数重构（7 个）
- `login/index.js` + `package.json`
- `toggle_favorite/index.js`（getMarks 修字段+报名、search、checkBatch、add 幂等、R1 收藏）
- `flashcards/index.js`（所有权、batchCreate、R6 搜索分页）
- `groups/index.js`（join 原子、setRemark 事务、disband 级联、invite_code 冲突重试）
- `schedules/index.js`（detail/signups/history 鉴权、signup 事务+signup_count、updateActivity 联动、resolveUid）
- `schedule_notify/index.js`（T1-T5 修正：时区/排序/锁/循环/按条置 sent/birthday_mmdd）
- `get_daily_quote/index.js`（C1-C4：TTL/forceRefresh/孤儿清理/配置中心 key）

### D. schema 权限收紧（12 个 .schema.json）
- `uni-id-users`、`uni-id-token`、`daily_data`、`user_favorites`、`flashcards`、`groups`、`group_events`、`schedules`、`schedule_history`、`signups`、`signup_drafts`、`opendb-open-data`
- 新增字段：`schedules.signup_count`、`uni-id-users.birthday_mmdd`、`daily_data.expireAt`、`user_favorites.content_hash`

### E. 页面注册与搜索
- `calendar/pages.json`（注册 search）
- `pages/search/search.vue`（改走云函数，删直查）

### F. 配置与依赖
- 各处 `package.json` 增加依赖（uni-id-common / uni-config-center）
- `uni-id.config.json`（M-1 密钥占位）

### 🎛 需用户在 uniCloud 控制台完成（本蓝本不覆盖）
- 创建 16+ 条索引（见 Phase 2.2，含 content_hash 调整）
- 配置云函数环境变量（TIANAPI_KEY 等）
- 配置定时触发器（cron 修正后）
- 部署云函数

---

## 三、修正后的分阶段执行清单

### Phase 1 身份与鉴权
- 📍 新增 `utils/auth.js`、`utils/cloud.js`（M-10：getUid 恒非空）
- 📍 12 页面迁移 getUserId→getUid/callApi
- 📍 `login` 接入 uni-id-common（M-1 前置：密钥轮换列为 Block）
- 📍 各业务云函数入口 `resolveUid`
- 验收：登录后 uid 恒不变；重登数据不丢；伪造 token→401

### Phase 2 数据库权限与索引
- 📍 12 schema 全收紧
- 📍 前端去直查（search.vue 等）
- 🎛 索引（M-8：user_favorites 用 content_hash 唯一索引；M-2：唯一索引延后到 Phase 7 后建）
- 验收：客户端 db.collection().get() 无权限；云函数内正常

### Phase 3 核心业务缺陷
- 📍 toggle_favorite：getMarks 修字段+报名；search；checkBatch（R1）；add 幂等
- 📍 search.vue 修复 + pages.json 注册
- 📍 收藏状态改批量（R1）
- 📍 getMarks 加 month（R3）
- 验收：日历打点含收藏+日程+报名；搜索页可搜卡片+收藏

### Phase 4 权限边界
- 📍 schedules.detail/signups/**history**（G-03）鉴权 + 脱敏
- 📍 flashcards 所有权
- 📍 groups 成员校验
- 验收：私有日程/名单/卡片他人→403

### Phase 5 一致性/并发/提醒
- 📍 signup 事务 + signup_count + cancelSignup 联动（G-04）+ updateActivity 联动
- 📍 toggle_favorite.add 用 content_hash + 冲突幂等
- 📍 groups.join 条件更新、setRemark 事务
- 📍 schedule_notify：时区（T3）/锁（T2）/排序+循环（T1）/按条置 sent（M-5/G-05）/birthday_mmdd
- 验收：并发不超卖；失败重发；生日定时器有效

### Phase 6 配置与可维护
- 📍 uni-config-center 收敛 template/key
- 📍 get_daily_quote：TTL（C1）/forceRefresh 修正（C2）/孤儿清理（C3）
- 📍 schedules 拆 lib/ 子模块
- 📍 disband 级联清理、废弃 group_ids
- 验收：grep 无硬编码密钥；cache 行为正确

### Phase 7 数据迁移
- 🎛 一次性 migrate_user_id 云函数（dryRun→执行）
- 📍 迁移脚本源码可提前编写

### Phase 8 回归与上线
- 📍 回归清单
- 🎛 上线顺序 + 回滚

---

## 四、本次动手范围（第一批落地）

为避免一次性改动过大难以验证，本次落地 **Phase 1（前端工具 + login 云函数 + resolveUid 骨架）+ Phase 2（schema 权限）+ Phase 3（页面注册/搜索）** 中所有标 📍 的**可编码**改动，并完成 D/E 的文件级改动。

依赖控制台的（索引、触发器、环境变量、部署、迁移）将明确标注为待办，由用户在 HBuilderX/控制台完成。

> 下文开始逐文件落地。每个文件改动后附带"改动说明"。
