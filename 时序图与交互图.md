# 每日一言小程序 — 时序图与交互图

> 技术栈：uni-app (Vue3) + uniCloud (阿里云) + uni-ui，目标平台：微信小程序
> 图例：前端页面（左侧）→ 云函数（中间）→ 数据库 / 外部服务（右侧），箭头即一次前后端交互。

---

## 一、系统架构交互图（前端 ↔ 云函数 ↔ 数据库 ↔ 外部服务）

```mermaid
flowchart TB
    subgraph WX["微信小程序客户端"]
        U["用户"]
    end

    subgraph FE["前端页面 pages/ (uni-app · Vue3)"]
        P1["首页 index.vue<br/>每日一言 swiper · 日历 · 收藏/知识/日程入口"]
        P2["登录 login.vue"]
        P3["我的收藏 favorites.vue"]
        P4["记忆卡片 anki.vue"]
        P5["创建卡片 create-card.vue"]
        P6["日程列表 schedule.vue"]
        P7["创建日程 create-schedule.vue"]
        P8["日程详情 schedule-detail.vue"]
        P9["发起报名 signup-create.vue"]
        P10["我的群组 groups.vue"]
        P11["群组详情 group-detail.vue"]
        P12["搜索 search.vue"]
    end

    subgraph CF["uniCloud 云函数 cloudfunctions/"]
        F1["login<br/>wxLogin · updateProfile"]
        F2["get_daily_quote<br/>聚合每日一言/新闻/历史"]
        F3["toggle_favorite<br/>list · getMarks · check · add · remove"]
        F4["flashcards<br/>create · list · search · updateStatus · delete"]
        F5["groups<br/>create · join · myGroups · detail · setRemark · events · leave · disband"]
        F6["schedules<br/>日程/报名/草稿/提醒/历史/订阅"]
        F7["schedule_notify<br/>定时触发器 · 日程/生日提醒"]
    end

    subgraph EXT["外部服务"]
        E1["微信 jscode2session"]
        E2["今日诗词 v2.jinrishici.com"]
        E3["一言 v1.hitokoto.cn"]
        E4["天行 API<br/>AI一言 · 新闻 · 历史今天"]
        E5["微信读书小程序"]
        E6["单向历小程序"]
        E7["微信订阅消息"]
    end

    subgraph DB["uniCloud 数据库 collections"]
        D1["uni-id-users 用户档案"]
        D2["uni-id-token 登录令牌"]
        D3["daily_data 每日内容缓存"]
        D4["user_favorites 收藏"]
        D5["flashcards 记忆卡片"]
        D6["groups 群组"]
        D7["group_events 群动态"]
        D8["schedules 日程"]
        D9["schedule_history 日程历史"]
        D10["signups 报名记录"]
        D11["signup_drafts 报名草稿"]
    end

    U <--> P1
    P1 -- "fetchJinrishici 直调" --> E2
    P1 -- "get_daily_quote" --> F2
    P1 -- "toggle_favorite check/getMarks/add/remove" --> F3
    P1 -- "navigateToMiniProgram 跳转" --> E5
    P1 -- "navigateToMiniProgram 跳转" --> E6
    P2 <--> F1
    P3 --> F3
    P4 <--> F4
    P5 --> F4
    P6 <--> F6
    P7 <--> F6
    P8 <--> F6
    P9 <--> F6
    P10 <--> F5
    P11 <--> F5
    P11 --> F6
    P12 --> F4
    P12 -- "JQL 直查" --> D4

    F1 --> E1
    F1 --> D1
    F1 --> D2
    F2 --> E3
    F2 --> E4
    F2 --> D3
    F3 --> D4
    F3 --> D8
    F4 --> D5
    F5 --> D6
    F5 --> D7
    F6 --> D8
    F6 --> D9
    F6 --> D10
    F6 --> D11
    F6 --> D6
    F6 --> D7
    F6 --> D1
    F7 --> E7
    F7 --> D8
    F7 --> D6
    F7 --> D7
    F7 --> D1
```

---

## 二、登录与档案（login）

> 前端 `uni.login` 取 code → 云函数换 openid → 建/查用户 → 发 token → 前端存储 → 补齐头像/昵称/生日。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant Login as 前端·登录页 login.vue
    participant WX as 微信平台
    participant CF as 云函数·login
    participant UDB as uni-id-users
    participant TDB as uni-id-token
    participant OS as uniCloud 云存储

    U->>Login: 点击「微信一键登录」
    Login->>WX: uni.login() 获取临时 code
    WX-->>Login: 返回 code
    Login->>CF: callFunction({action:'wxLogin', code})
    CF->>WX: jscode2session(appid+secret+code)
    WX-->>CF: 返回 openid / unionid
    CF->>UDB: 按 openid 查找用户
    alt 新用户
        CF->>UDB: add 创建（昵称=微信用户, isNew=true）
    else 老用户
        CF->>UDB: update 刷新登录信息
    end
    CF->>TDB: add 生成 SHA256 token 入库
    CF-->>Login: 返回 {uid, token, nickname, isNew}
    Login->>Login: 本地缓存 token / user_info / uid
    alt 新用户或未填昵称，引导补齐档案
        U->>Login: chooseAvatar + 输入昵称 + 选择生日
        Login->>OS: uniCloud.uploadFile 上传头像 → 返回 fileID
        Login->>CF: callFunction({action:'updateProfile', nickname, avatar, birthday})
        CF->>UDB: update 写回档案
        CF-->>Login: 已更新
    end
    Login-->>U: 返回首页（游客模式仅本地 visitor_id，不同步云端）
```

---

## 三、每日一言加载（get_daily_quote）

> 前端 `Promise.all` 并行：直调今日诗词 + 云函数聚合天行 AI/新闻/历史，云函数按 `date` 走 `daily_data` 缓存，未命中才回源并写缓存。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant Idx as 前端·首页 index.vue
    participant JR as 今日诗词 v2.jinrishici.com
    participant CF as 云函数·get_daily_quote
    participant TQ as 天行 API(AI/新闻/历史)
    participant HK as 一言 hitokoto.cn
    participant F3 as 云函数·toggle_favorite
    participant DD as daily_data 缓存

    U->>Idx: 打开首页 / 日历切换日期
    Idx->>Idx: initDateDisplay 展示选中日期
    par Promise.all 并行聚合
        Idx->>JR: uni.request(X-User-Token) 前端直调
        JR-->>Idx: 诗词 {content, dynasty, author, title}
        Idx->>CF: callFunction({date})
        CF->>DD: 按 date 查缓存
        alt 命中缓存（api_cache 且非强制刷新）
            CF-->>Idx: 直接返回缓存 items
        else 未命中
            par 云函数并行回源 4 个第三方接口
                CF->>HK: GET v1.hitokoto.cn（一言）
                CF->>TQ: GET tianapi ai/index（AI 文案）
                CF->>TQ: GET tianapi generalnews/index（新闻）
                CF->>TQ: GET tianapi lishi/index?date=MMdd（历史今天）
            end
            CF->>DD: upsert 当日缓存（首条写 quote 字段）
            CF-->>Idx: 返回聚合数组
        end
    end
    Idx->>Idx: 排序组装 quoteList（诗词→单向历→AI/新闻→历史）
    Idx->>F3: toggle_favorite.check 逐卡核对是否已收藏
    Idx-->>U: swiper 渲染（收藏/微信读书/单向历操作栏 + 今日思考）
```

---

## 四、收藏与日历打点联动（toggle_favorite）

> 收藏写入 `user_favorites`，日历打点来自「收藏日期 ∪ 个人日程日期」，实现收藏 ↔ 日历双向联动。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant Idx as 首页 index.vue
    participant Fav as 我的收藏 favorites.vue
    participant CF as 云函数·toggle_favorite
    participant FB as user_favorites
    participant SC as schedules

    U->>Idx: 点击卡片「收藏」
    Idx->>CF: callFunction({action:'add', user_id, date, content, author, source})
    CF->>FB: 查重 → 无则 add 插入
    CF-->>Idx: {code:200, isFavorited:true}
    Idx->>CF: callFunction({action:'getMarks', user_id})
    CF->>FB: 查所有收藏日期
    CF->>SC: 查所有个人日程日期
    CF-->>Idx: 返回 marks[{date, info}]
    Idx->>Idx: 组装 uni-calendar :selected 红点

    U->>Idx: 日历弹窗选中某天 / 切换月份
    Idx->>CF: callFunction({action:'getMarks'})
    CF-->>Idx: 返回标记日期
    Idx->>CF: callFunction({action:'get_daily_quote', date}) 重新拉当天内容
    Idx->>Idx: loadCalendarMarks() 刷新打点

    U->>Fav: 打开「我的收藏」
    Fav->>CF: callFunction({action:'list', user_id})
    CF->>FB: 查全部收藏（createTime 倒序）
    CF-->>Fav: 按日期分组渲染 + 本地关键词过滤
    U->>Fav: 点击「取消收藏」
    Fav->>CF: callFunction({action:'remove', date, content})
    CF->>FB: 匹配记录 remove
    CF-->>Fav: 本地移除该条
```

---

## 五、记忆卡片（flashcards）

> 单卡创建 + `---` 批量导入 → `flashcards` 集合；复习/知识库双 tab 切换状态。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant Anki as 前端·记忆卡片 anki.vue
    participant CC as 前端·创建卡片 create-card.vue
    participant P12 as 前端·搜索页 search.vue
    participant CF as 云函数·flashcards
    participant FD as flashcards 集合

    U->>CC: 输入正面/背面/分类
    CC->>CF: callFunction({action:'create', user_id, front, back, deck, status:'review'})
    CF->>FD: add 插入卡片
    CF-->>CC: {code:200, id}

    U->>CC: 粘贴多行「正面 --- 背面」批量导入
    loop 逐行解析「正面 --- 背面」
        CC->>CF: callFunction({action:'create', front, back})
        CF->>FD: add
    end
    CC-->>U: 提示「成功导入 N 张卡片」

    U->>Anki: 切到「复习」tab
    Anki->>CF: callFunction({action:'list', user_id, status:'review'})
    CF->>FD: where(user_id, status='review') 查询
    CF-->>Anki: 复习卡片列表
    Anki-->>U: 3D 翻转卡片 + textarea 自测对比

    U->>Anki: 切到「知识库」tab
    Anki->>CF: callFunction({action:'list', user_id})
    CF-->>Anki: 全部卡片（本地关键词过滤）
    U->>Anki: 加入复习 / 移出复习
    Anki->>CF: callFunction({action:'updateStatus', card_id, new_status})
    CF->>FD: update status（review ↔ library）
    U->>Anki: 删除卡片
    Anki->>CF: callFunction({action:'delete', card_id})
    CF->>FD: remove

    U->>P12: 全局搜索「卡片和收藏」
    P12->>CF: callFunction({action:'search', keyword})
    CF->>FD: 全量拉取后本地过滤正/反面包含
    CF-->>P12: 返回匹配卡片
```

---

## 六、群组协作（groups）

> 邀请码建群/入群 → 群详情（成员档案/备注/生日）→ 群动态 → 退出/解散（级联清理）。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant GP as 前端·我的群组 groups.vue
    participant GD as 前端·群组详情 group-detail.vue
    participant CF as 云函数·groups
    participant GB as groups 集合
    participant EV as group_events 集合
    participant F6 as 云函数·schedules
    participant DB as schedules/signups 集合

    U->>GP: 点击「创建群组」
    GP->>CF: callFunction({action:'create', group_name, description, group_remark})
    CF->>GB: add 建群 + 生成 6 位邀请码 + 群主成员
    CF->>EV: add 记录 member_joined
    CF-->>GP: 返回群信息

    U->>GP: 输入 6 位邀请码「加入」
    GP->>CF: callFunction({action:'join', invite_code, group_remark})
    CF->>GB: 校验邀请码 → 成员数组 push
    CF->>EV: add 记录 member_joined
    CF-->>GP: 返回结果并刷新列表

    U->>GP: 点击群卡片 → 群详情
    GD->>CF: callFunction({action:'myGroups', user_id})
    GD->>CF: callFunction({action:'detail', group_id})
    CF->>GB: 查群 + 成员档案名/备注/生日
    GD->>CF: callFunction({action:'events', group_id})
    CF->>EV: 查群动态事件流
    GD->>F6: schedules.getGroupSchedules 查群日程
    GD-->>U: 展示成员/日程/动态，可复制邀请码

    U->>GD: 修改群内备注（自己 / 群主改他人）
    GD->>CF: callFunction({action:'setRemark', target_user_id, group_remark})
    CF->>GB: 更新成员备注字段
    CF-->>GD: 刷新详情

    alt 非群主退出
        U->>GD: 点击「退出群组」
        GD->>CF: callFunction({action:'leave'})
        CF->>GB: 移除成员
        CF->>EV: add 记录 member_left
    else 群主解散
        U->>GD: 点击「解散群组」
        GD->>CF: callFunction({action:'disband'})
        CF->>GB: remove 群
        CF->>DB: 级联删除该群的 schedules / signups / group_events
    end
```

---

## 七、日程 · 报名 · 提醒（schedules + schedule_notify）

> 核心链路：创建/编辑日程 → 发起报名（可草稿）→ 群成员填问卷报名 → 名单/统计/动态 → 定时触发器推送订阅消息。

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant SC as 日程列表 schedule.vue
    participant CS as 创建日程 create-schedule.vue
    participant SGC as 发起报名 signup-create.vue
    participant SD as 日程详情 schedule-detail.vue
    participant CF as 云函数·schedules
    participant SB as schedules 集合
    participant SH as schedule_history
    participant SU as signups 集合
    participant DF as signup_drafts
    participant EV as group_events
    participant UDB as uni-id-users
    participant NT as 定时·schedule_notify
    participant MSG as 微信订阅消息

    U->>SC: 打开日程页
    SC->>CF: callFunction({action:'getMonth', month})
    CF->>SB: 查当月个人+群日程（附带状态/报名数/我的报名状态）
    CF-->>SC: 今日提醒 + 日程列表（范围/状态筛选）
    U->>SC: 关键词搜索
    SC->>CF: callFunction({action:'search', keyword})

    U->>CS: 选模板/时间/定时提醒/发布范围
    CS->>CF: groups.myGroups 拉取可选群组
    opt 选择定时提醒
        CS->>MSG: uni.requestSubscribeMessage 授权提醒模板
    end
    CS->>CF: callFunction({action:'create', scope, group_id, signup_form, sessions, reminders, member_name})
    CF->>SB: createScheduleDoc 构建写入（计算 next_remind_at）
    CF->>SH: add 记录 create 历史
    opt 发布到群
        CF->>EV: add 记录 schedule_created
    end
    CF-->>CS: 返回 id → 回列表/详情

    U->>SGC: 发起报名（模板/问卷/场次/名额）
    opt 先存草稿
        SGC->>CF: callFunction({action:'saveSignupDraft'})
        CF->>DF: upsert 草稿
        SGC->>CF: listSignupDrafts / deleteSignupDraft
    end
    SGC->>CF: callFunction({action:'publishSignup', group_id, form, sessions, quota, deadline})
    CF->>SB: 创建群日程（signup_enabled=true）
    CF->>SH: add 历史
    CF->>EV: add 记录 signup_published
    CF-->>SGC: 返回 schedule_id → 跳转详情

    U->>SD: 打开日程详情（schedule_id 参数）
    SD->>CF: callFunction({action:'detail', schedule_id})
    CF->>SB: 查日程 + 报名统计 + 我的报名状态 + 问卷预填
    SD->>CF: callFunction groups.detail 校验是否群主（决定编辑/管理权限）
    par 并行加载
        SD->>CF: callFunction({action:'signups'}) → 名单
        SD->>CF: callFunction({action:'history'}) → 变更动态
    end
    CF-->>SD: 渲染 报名/名单/统计/动态 四 Tab + 名额进度

    U->>SD: 填写问卷提交报名
    SD->>CF: callFunction({action:'signup', form_data})
    CF->>SB: 校验群成员/已关闭/已截止/必填/场次名额/总名额
    alt 已报名过
        CF->>SU: 更新为 signed（保留记录）
    else 新报名
        CF->>SU: add（status='signed', form_data）
    end
    opt 问卷含生日字段且写档案
        CF->>UDB: 报名生日写回用户档案
    end
    CF->>EV: add 记录 signup_added
    CF->>MSG: sendSignupNotify 通知发起人
    opt 满员
        CF->>SB: 自动置 signup_closed=true
    end
    CF-->>SD: 报名成功

    U->>SD: 取消报名 / 群主移除报名
    SD->>CF: callFunction({action:'cancelSignup'})
    CF->>SU: status='cancelled'（若满员则重新开放报名）
    CF->>EV: add 记录 signup_cancelled

    U->>SD: 活动设置（群主）
    SD->>CF: callFunction({action:'updateActivity', title, quota, form, sessions, closed, deadline})
    CF->>SB: 更新报名配置
    CF->>SH: add 历史

    U->>SD: 开启「报名提醒」
    SD->>MSG: uni.requestSubscribeMessage(结果模板)
    SD->>CF: callFunction({action:'subscribeNotify', enabled})
    CF->>UDB: 写入 subscribe_templates
    SD->>CF: getNotifyState 读取授权状态

    Note over NT,MSG: ─── 定时触发（schedule_notify 触发器）───
    NT->>SB: 扫描 next_remind_at ≤ now 的日程
    NT->>UDB: 查询已订阅模板的接收者 openid
    NT->>MSG: sendSubscribeMessage 发送日程提醒
    NT->>EV: 记录 schedule_starting（去重）
    Note over NT,UDB: 每天 09:00 生日触发器
    NT->>UDB: 匹配当天生日用户
    NT->>MSG: 向群成员发送生日祝福提醒
    NT->>EV: 记录 birthday_notify（去重）
```

---

## 八、数据库集合与业务映射速查

| 集合 | 写入来源云函数 | 读取页面 | 核心字段 |
|---|---|---|---|
| `uni-id-users` | login / schedules(报名沉淀生日) | login / 群组 / 搜索 | openid, nickname, avatar, birthday, phone, subscribe_templates |
| `uni-id-token` | login | login | user_id, token |
| `daily_data` | get_daily_quote | index(经云函数) | date, quote, author, source, items |
| `user_favorites` | toggle_favorite | index / favorites / search | user_id, date, content, author, source |
| `flashcards` | flashcards | anki / create-card / search | user_id, front, back, deck, status |
| `groups` | groups | groups / group-detail / create-schedule / signup-create / schedules | group_name, creator_id, invite_code, members[] |
| `group_events` | groups / schedules / schedule_notify | group-detail | group_id, type, user_id, data |
| `schedules` | schedules / schedule_notify | schedule / schedule-detail / index(打点) | title, date, scope, group_id, signup_*, sessions, reminders, next_remind_at |
| `schedule_history` | schedules | schedule-detail | schedule_id, action, before, after, changed_by |
| `signups` | schedules | schedule-detail | schedule_id, group_id, user_id, status, form_data, signed_at |
| `signup_drafts` | schedules | signup-create | user_id, group_id, title, form, sessions |
