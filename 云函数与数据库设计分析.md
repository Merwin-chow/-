# 云函数与数据库设计分析报告

> 分析时间：2026-08-16
> 分析范围：`calendar/uniCloud-aliyun/cloudfunctions/*`（7 个云函数）与 `calendar/uniCloud-aliyun/database/*.schema.json`（13 个 schema），并对照前端 `pages/` 实际调用与根目录设计文档（`开发方案思考.md`、`时序图与交互图.md`、`项目规划.md`）。
> 说明：本报告仅分析，不对任何代码与文档做修改。

---

## 一、总体架构概览

技术栈：uni-app (Vue3) + uniCloud（阿里云）+ uni-ui，目标微信小程序。

```
前端 pages/ ──callFunction──▶ 云函数 cloudfunctions/ ──uniCloud.database()──▶ 集合 collections
                                   │                          │
                                   └── httpclient ──▶ 外部 API（天行/hitokoto/微信）
```

现有 7 个云函数、11 个业务集合 + `uni-id` 体系 2 张表 + `opendb-open-data`：

| 云函数 | 行数 | 主要 action |
|---|---|---|
| login | 155 | wxLogin / phoneLogin / updateProfile / getCurrentUser |
| get_daily_quote | 200 | 聚合 hitokoto + 天行 AI/新闻/历史，写 `daily_data` 缓存 |
| toggle_favorite | 80 | list / getMarks / check / add / remove |
| flashcards | 66 | create / list / search / updateStatus / delete |
| groups | 205 | create / join / myGroups / detail / setRemark / events / leave / disband |
| schedules | 947 | getMonth / getDay / search / create / publishSignup / signup / update / delete / detail / signups / history 等约 20 个 |
| schedule_notify | 304 | 定时触发器：日程提醒 + 生日通知 |

---

## 二、云函数设计分析

### 2.1 `login`

**设计定位**：登录、档案更新、用户查询。自研 token（非 uni-id SDK）。

**问题：**

1. **【严重】`getCurrentUser` 用 token 当文档 `_id` 查询用户**（`login/index.js:129`）
   ```js
   const userRes = await db.collection('uni-id-users').doc(token).get()
   ```
   `token` 是 SHA256 字符串，`uni-id-users` 文档 `_id` 是用户 ID，二者完全不同 → 该分支永远返回 404。正确做法应先在 `uni-id-token` 按 token 查到 `user_id`，再查用户。
2. **【严重】`phoneLogin` 同样把 token 当 `_id` 用**（`login/index.js:94`）`db.collection('uni-id-users').doc(event.token).update({ phone })`，绑定手机号永远失败。
3. **【严重】身份体系断裂（前端侧）**：`login` 返回 `{ uid, token }`（`login/index.js:78`），前端同时存储 `uni_id_token`(token) 与 `current_user_id`(uid)（`login.vue:158-160`）。但几乎所有页面的 `getUserId()` 优先返回 `uni_id_token`（如 `index.vue:221-222`、`schedule.vue:153-154`、`anki.vue:138-139`、`favorites.vue:84-86`、`create-schedule.vue:401-403`、`groups.vue:101`、`group-detail.vue:156`、`schedule-detail.vue:416`、`signup-create.vue:229`、`create-card.vue:73`）。而 `wxLogin` **每次登录都会新生成一个 token 并入库**（`login/index.js:65-72`），token 是随机的、与用户非稳定绑定 → 用户每次重新登录后 `user_id` 变化，所有按 `user_id` 归属的数据（收藏、卡片、日程、群、报名）都"失联"，表现为重新登录后数据丢失。应统一以 `uid` 作为业务 user_id。
4. **【高】无 token 鉴权**：云函数信任客户端传来的 `user_id`，`getCurrentUser` 之外没有任何 token 校验；`uni-id-token` 里 token 永不校验、永不过期、永不清理，每次登录追加一条 → 表无限增长，配置里的 `tokenExpiresIn`（`uni-id.config.json`）未生效。
5. **【高】密钥硬编码入库**：`login/config.json` 明文存微信 `appid/appsecret`（提交进仓库），且与 `uni-id.config.json` 中的 appid/appsecret 重复、两处来源易漂移。
6. **【中】`updateProfile` 无登录态校验**，任何人只要知道 uid 就能改他人昵称/头像/生日（`login/index.js:100-110`）。

### 2.2 `get_daily_quote`

**设计定位**：聚合每日一言/新闻/历史，写 `daily_data` 缓存，未命中才回源。

**问题：**

1. **【中】缓存无 TTL、无用户维度**：全局按 `date` 一个 api_cache 文档，永不过期。过去日期内容永久停留在首次抓取结果（AI 文案/新闻会一直显示旧内容，若设计意图是"时光机"则合理，但应明确策略）；历史日期也永远不会被刷新。
2. **【中】多个 api_cache 文档共存时只 upsert 第一个**（`get_daily_quote/index.js:154-157`），若某天重复写入会产生多份缓存文档，其余成为孤儿。
3. **【高】密钥硬编码**：`TIANAPI_KEY` 写死在源码（`get_daily_quote/index.js:1`），今日诗词 Token 写在前端 `index.vue:155`（小程序包内可被提取，Token 泄露风险）。
4. **【低】实现与方案文档不一致**：`开发方案思考.md` 表格列出「天行经典台词 🎬 dialogue」，实际代码未接入 dialogue，而是用 hitokoto 代替（`get_daily_quote/index.js:23`）。方案文档四节的接口清单（dialogue）与实现（hitokoto）脱节。
5. **【低】`todayCn` 用 UTC+8 的 ISO 字符串**（`get_daily_quote/index.js:4-6`）——当前正确，但依赖时区偏移硬编码。
6. **【低】hitokoto 的 `author` 取 `from`（出处）而非 `from_who`（作者）**，语义偏差（`get_daily_quote/index.js:30`）。
7. **【低】空结果兜底文案返回给前端但前端会再包一层 fallback**（`index.vue:303-306`），双兜底逻辑可简化。

### 2.3 `toggle_favorite`

**设计定位**：收藏管理 + 日历打点（收藏日期 ∪ 个人日程日期）。

**问题：**

1. **【严重】`getMarks` 查日程用了错误字段**（`toggle_favorite/index.js:24-27`）
   ```js
   const allSchedules = await db.collection('schedules').where({ user_id }).field({ date: true }).get()
   ```
   `schedules` 文档没有 `user_id` 字段，创建者是 `owner_id`（`schedules/index.js:287`）。→ 日程打点永远为空，日历上永远只显示收藏红点，不显示日程。时序图文档（第四节）声称"收藏日期 ∪ 个人日程日期"，实现与设计不符。
2. **【高】无所有权校验**：`list/remove` 只凭客户端传的 `user_id`，无 token 校验，可越权读/删他人收藏。
3. **【中】查重非原子**：`add` 先查后插（`toggle_favorite/index.js:52-68`），无唯一索引，并发点击可重复收藏；`remove` 用 `where().remove()` 可一次删多条同名记录。
4. **【中】list 无分页**，用户收藏超 100 条时 `get()` 默认上限会截断。
5. **【低】`check` 要求同时传 `date`+`content` 精确匹配**，内容换行/格式变化会导致重复收藏判定失效。

### 2.4 `flashcards`

**设计定位**：记忆卡片 CRUD + 搜索。

**问题：**

1. **【严重】无所有权校验**：`updateStatus` / `delete` 只凭 `card_id`（`flashcards/index.js:52-63`），任意用户可删改他人卡片；`list` 虽按 `user_id` 过滤，但 `user_id` 来自客户端可伪造。
2. **【中】`search` 只拉前 500 条再内存过滤**（`flashcards/index.js:41`），卡片超过 500 张时搜索结果不完整；且关键词过滤在云函数内存做，无数据库索引。
3. **【中】`updateStatus` 未校验 `new_status` 枚举**，可写入任意值；`status` 字段无 schema 枚举约束。
4. **【低】`deck` 默认值'默认'，批量导入时逐条 create（N 次云函数调用）**，批量导入效率低、易触发频率限制（设计文档提到"极简批量导入"，应做批量 add）。

### 2.5 `groups`

**设计定位**：群组协作 + 事件日志。

**问题：**

1. **【高】`disband` 级联清理不完整**（`groups/index.js:188-192`）：删除群 + `schedules`(group) + `signups` + `group_events`，但**遗留** `schedule_history`、`signup_drafts`，也未清理 `uni-id-users.group_ids`（虽然该字段实际从未被写入）。
2. **【高】`join` 非原子 push**（`groups/index.js:62-70`）：并发加入时 `db.command.push` 可能产生重复成员记录（无唯一约束），且已加入判断与 push 之间非原子。
3. **【中】`setRemark` 整体替换 `members` 数组**（`groups/index.js:129-135`）：并发修改备注会覆盖他人变更（读-改-写无原子操作）。
4. **【中】邀请码生成无冲突校验**（`groups/index.js:198-204`）：6 位随机字符（去易混字符后 32^6≈10 亿），虽碰撞概率低但未做查重重试。
5. **【中】`myGroups` 用 `members.user_id` 点查**（`groups/index.js:79`），无索引全表扫描；`events` 无分页（limit 100）。
6. **【低】`uni-id-users.group_ids` 字段（schema 有）在 `groups` 所有 action 中从未维护** → 死字段，且与 `groups.members` 双写容易不一致。

### 2.6 `schedules`（最大单体，947 行）

**设计定位**：日程 + 报名 + 订阅消息 + 历史，核心业务。

**问题：**

1. **【严重】`detail` 对私有日程无所有权校验**（`schedules/index.js:897-905`）：只对 group 日程校验群成员，private 日程任何人只要知道 `schedule_id` 就能查看详情。
2. **【严重】`signups` 列表无成员校验且返回敏感信息**（`schedules/index.js:858-894`）：任何人不需是群成员即可拉取某活动的报名名单，且名单附带 `profile_birthday`、`profile_phone`（手机号/生日）→ PII 泄露风险。
3. **【高】名额/场次校验非原子（竞态）**（`schedules/index.js:709-715`、`701-706`、`752-760`）：先 `count` 后 `add`，高并发下可超名额（项目规划文档声称"利用原子操作确保高并发报名统计准确"，实现未达到）。应使用事务或 `signup_closed`/配额字段的原子条件更新。
4. **【高】`publishSignup` 后 `signup_quota` 为 0 时不触发满员逻辑**（正确），但若创建后手动把 `signup_quota` 设为 0 且已有报名，不重新开放/关闭逻辑边界混乱。
5. **【中】订阅模板 ID 与 DCLOUD_APPID 硬编码**（`schedules/index.js:193-197`），且与 `schedule_notify` 中 `TEMPLATE_ID` 重复（同一模板 ID 出现在两处）；`sendSubMsg` 的 page 固定写死 `pages/schedule/schedule`。
6. **【中】订阅消息发送全部吞错**（`schedules/index.js:220`、`.catch(()=>{})`），失败无重试、无日志归因，用户"授权了却没收到提醒"难以排查。
7. **【中】`attachSignupCounts` 用 `cmd.in(ids)` 一次查询**（`schedules/index.js:329`）：`getMonth` 最多拉 500 条日程（`limit(500)`），而 uniCloud `in` 运算有数量上限，日程量大时会报错。
8. **【中】`getMyGroupIds` limit(100)**（`schedules/index.js:167`）：用户加入超过 100 个群时其余群日程不可见。
9. **【中】单体过大、action 分支过多**（约 20 个 if）：职责不单一，难以测试与维护；部分校验逻辑重复（`computeStatus` 前端与云函数各实现一份，见 `schedule-detail.vue`）。
10. **【中】`update` 允许把 group 日程改成 private 时保留 `group_id` 语义不清**（`schedules/index.js:548-550`），`finalScope` 分支里 private→group 才需要 group_id，反向转换时旧 `group_id` 被清空但相关 signups 未清理。
11. **【低】`delete` 只删 signups，残留 `schedule_history` 与 `group_events` 引用**（`schedules/index.js:630-635`）。
12. **【低】生日日程 `getBirthdays` 依赖 `type='birthday'` 约定**（`schedules/index.js:931-934`），与 `schedule_notify` 的生日通知（按用户 `birthday` 字段）是两套逻辑，容易重复/遗漏。

### 2.7 `schedule_notify`（定时触发器）

**设计定位**：扫描 `next_remind_at` 发送订阅消息 + 生日通知。

**问题：**

1. **【中】每次只处理 100 条且无排序**（`schedule_notify/index.js:132-137`）：到期日程 >100 时同轮会延迟处理（下轮轮询补上，可接受但无保证）；`where next_remind_at ≤ now` 无 orderBy，处理顺序随机。
2. **【高】发送失败被标记为已发送**（`schedule_notify/index.js:155-163`）：循环里把到点 reminder 的 `sent=true` 后才发送，`sendMsg` 失败时不回滚 → 用户永久错过该提醒。应成功后才置 sent。
3. **【中】生日通知全表正则匹配**（`schedule_notify/index.js:250`）：`birthday: RegExp(\d{4}-MMdd)` 无索引、每次全集合扫描，且 `.limit(100)` 会漏掉第 101+ 个当天生日用户；群成员再拉取也是逐群 N+1 查询。
4. **【中】`getRecipientOpenids` 对报名活动只通知已报名用户**（`schedule_notify/index.js:220-225`）：群日程若开放报名但用户未报名则不提醒，与"群成员提醒"语义可能不符（需业务确认）。
5. **【低】去重依赖 `group_events` 查询**（`schedule_notify/index.js:194-206`）：若 `group_events` 写入失败则可能重复发送。

---

## 三、数据库表设计分析

### 3.1 权限模型（全库普遍问题）

**【严重】几乎所有 schema 的 `permission` 均为全开（`read/create/update/delete: true`）**，包括：

- `uni-id-users.schema.json:4-9`（read:true → 任意客户端 JQL 可读所有用户 openid、昵称、手机号、生日）
- `user_favorites`、`flashcards`、`schedules`、`signups`、`groups` 等全部 read:true

`read: true` 表示任意客户端可通过 `uniCloud.database()`（JQL）直查集合，`search.vue:95-97` 正是用客户端 `db.collection('user_favorites')` 直查。→ 他人收藏、手机号、生日均可被读取；云函数的存在意义被绕过。应改为 `false` 或基于 `user_id`/`owner_id` 的权限表达式，全部读写走云函数。

`opendb-open-data.schema.json` 权限全 false，是正确的对照组。

### 3.2 各集合分析

| 集合 | 设计合理点 | 问题 |
|---|---|---|
| `uni-id-users` | 归档必要字段齐全 | ① read/create/update 全开（PII 泄露）；② `group_ids` 字段无人维护（死字段）；③ `openid` 无唯一索引；④ 游客身份不入库、但 `login.getCurrentUser` 分支错误导致游客态与登录态割裂 |
| `uni-id-token` | 记录登录令牌 | ① 每次登录追加、无过期清理、`tokenExpiresIn` 配置未生效；② 无 token↔user 反查索引（云函数校验时无法快速定位）；③ permission delete:true 客户端可删 |
| `daily_data` | 按 date 缓存聚合 items 设计合理 | ① 无 TTL、无唯一索引（date 可重复）；② `question/answer` 仅手动文档有值，语义与 api_cache 混在同一集合；③ items 内嵌套结构未限制字段 |
| `user_favorites` | 字段够用 | ① 无 (user_id, date, content) 唯一索引（查重靠应用层）；② read:true 公开可读他人收藏；③ `user_id` 实际存的是 token 哈希（身份体系断裂的直接后果）；④ list 无分页 |
| `flashcards` | 简洁 | ① 无 (user_id, status) 索引；② status 无枚举约束；③ front/back 不支持图片（`项目规划.md` 提到图片支持未落地） |
| `groups` | members 内嵌数组 + events 分离，合理 | ① `invite_code` 无唯一索引；② members 无成员上限；③ `members.user_id` 点查无索引；④ members 与 `uni-id-users.group_ids` 双向冗余且未同步 |
| `group_events` | 只读事件流，update/delete:false 正确 | ① 无 (group_id, type) 索引；② 无保留策略，只增不删、无分页（前端 limit 100） |
| `schedules` | 字段覆盖全面，`next_remind_at` 专为定时索引设计（好） | ① `signup_fields`（旧）与 `signup_form`（新）双字段并存，历史包袱；② 无 (owner_id, date)、(date, next_remind_at)、(group_id, scope) 索引；③ read:true 公开可读他人日程；④ `reminders.sent` 由定时器改写，与主文档同文档写冲突 |
| `schedule_history` | 完整 before/after 快照，审计友好 | ① 存储整个文档快照体积大、只增不删；② 无按 schedule_id 索引说明；③ 无保留策略 |
| `signups` | form_data 灵活 | ① 无 (schedule_id, user_id) 唯一索引 → 并发/重复报名风险（报名逻辑依赖应用层去重）；② read:true 可直读他人报名数据；③ status 无枚举约束 |
| `signup_drafts` | 草稿独立，合理 | ① read:true；② 无清理策略（草稿永不过期） |

---

## 四、跨模块关键问题汇总（按优先级）

### P0 — 必须修复（影响核心功能正确性 / 数据安全）

1. **身份体系断裂**：前端 `getUserId()` 返回 `uni_id_token`（每次登录变一次）而非 `uid`，导致全部用户数据按 token 哈希归档、重登后"丢失"。（`login/index.js:65-78` + 全部页面 `getUserId`）
2. **无鉴权 + 客户端可信**：所有云函数信任 `event.user_id`，任意可伪造；`getCurrentUser`/`phoneLogin` 用 token 当 `_id` 必然失败。（`login/index.js:94,129`）
3. **schema 权限全开**：`uni-id-users`、`user_favorites`、`schedules`、`signups` 等 read:true，客户端 JQL 可直读 PII/他人数据。
4. **`getMarks` 日程字段用错**（`user_id` vs `owner_id`），日历日程打点永远为空。（`toggle_favorite/index.js:24-27`）
5. **`search.vue` 调用不存在的云函数 `uni-id-users`**，搜索页永远游客态；且该页未注册进 `pages.json`。（`search.vue:60-63`、`pages.json`）

### P1 — 应尽快修复（数据一致性 / 权限边界）

6. `signups`/`schedules.detail` 对私有/报名名单无权限校验，泄露手机号/生日。
7. 报名名额/场次校验非原子（count→add），高并发超名额。
8. `flashcards.updateStatus/delete` 无所有权校验。
9. `schedule_notify` 发送失败仍置 sent → 丢提醒；生日/到期扫描 limit 100 会漏。
10. 密钥/Token/AppSecret 硬编码入库并随小程序分发。

### P2 — 建议优化（性能 / 可维护性）

11. 全部集合补索引（openid、date、user_id、schedule_id、invite_code、next_remind_at 等）。
12. `schedules` 947 行拆分（日程 / 报名 / 订阅消息子模块）。
13. `groups` 并发：join 原子性、setRemark 用条件更新、invite_code 查重。
14. 级联清理补全（disband/delete 清理 schedule_history、signup_drafts、group_ids）。
15. 统一订阅模板 ID 与 DCLOUD_APPID 到环境变量/配置文件。
16. `uni-id.config.json` 的 token 过期/校验落地（或改走 uni-id SDK）。
17. `daily_data` 缓存增加 TTL/版本字段；历史日期刷新策略明确化。
18. 批量导入卡片改批量 add；分页统一。

---

## 五、设计文档与实现一致性对照

| 设计文档声明 | 实现现状 | 一致性 |
|---|---|---|
| 云函数聚合「天行经典台词 dialogue」`开发方案思考.md` | 实际用 hitokoto，未接 dialogue | ❌ 不一致 |
| 日历打点 = 收藏日期 ∪ 个人日程日期 `时序图四` | getMarks 用 `user_id` 查 schedules（字段不存在），日程打点恒空 | ❌ 不一致 |
| 报名高并发"原子操作确保统计准确" `项目规划四` | count→add 非原子，可超名额 | ❌ 不一致 |
| 搜索页 `search.vue` 存在且可全局搜索 `时序图 P12` | 页面存在但未注册 pages.json；且调用错误云函数 | ⚠️ 半成品 |
| 订阅消息授权存 `uni-id-users.subscribe_templates` `时序图七` | 已实现（schedules/index.js:516-537） | ✅ 一致 |
| Anki 卡片支持图片 `项目规划三` | 仅文本 front/back | ⚠️ 未落地（MVP 可接受） |
| 云函数 `get_daily_quote` 按 date 缓存 `daily_data` `时序图三` | 已实现 | ✅ 一致 |
| 群组解散级联清理 `时序图六` | 删 schedules/signups/group_events，遗留 history/drafts | ⚠️ 部分一致 |

---

## 六、结论

整体架构是合理的 uniCloud 云函数模式：单一 `exports.main` + `event.action` 路由、文档型集合映射业务实体、`daily_data` 缓存层、`group_events` 只读事件流、`next_remind_at` 定时字段都是好设计。

但存在 **4 个 P0 级缺陷**（身份体系断裂、无鉴权、schema 全公开、日历打点字段错）会让核心功能在真实登录后出错、且存在严重隐私泄露风险，建议按 P0→P2 顺序优先整改。本报告暂未修改任何代码或文档。
