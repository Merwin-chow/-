# 整改方案检查报告（覆盖性与缺口核查）

> 版本：v1.0 ｜ 日期：2026-08-16
> 检查对象：《云函数与数据库整改方案.md》（下称"方案"）
> 依据：《云函数与数据库设计分析.md》（下称"分析"）、《整改方案测试方案与用例.md》
> 方法：逐条对照分析文档 P0/P1/P2 问题清单，核查方案是否有对应整改动作、是否闭环、是否引入新风险；并结合 uniCloud 平台能力与实际代码复核。

---

## 一、检查结论速览

| 结论 | 说明 |
|---|---|
| ✅ 总体有效 | 方案对 4 个 P0、5 个 P1 均有对应整改动作，方向正确 |
| ⚠️ 3 处方案内部缺陷 | ①唯一索引创建时机与迁移去重冲突；②`signup_count` 未在取消报名时递减；③收藏唯一索引用长文本 `content` 有索引 key 超限风险 |
| ⚠️ 3 处遗漏 | ①`schedules.history` 未做鉴权；②`attachSignupCounts` 的 `cmd.in` 超限风险未覆盖；③游客/半登录态空 `uid` 边界未明确 |
| ⚠️ 2 处需决策 | ①JWT 删除 DB 记录≠真正吊销 token；②今日诗词改走云函数代理与既定"前端直调"架构冲突 |
| ✅ 结论 | 方案可执行，建议按本报告第四节补充整改后，再进入实施与测试阶段 |

---

## 二、问题覆盖矩阵

> 状态：✅=已闭环；🟡=基本覆盖但有缺口；❌=未覆盖；➕=方案新引入的补充项。

### 2.1 P0 级问题

| 问题 | 方案章节 | 状态 | 核查说明 |
|---|---|---|---|
| P0-1 身份断裂（前端把 token 当 user_id） | 1.1 utils/auth、1.3 resolveUid、Phase 7 迁移 | ✅ | 前端统一 `getUid()` + 云函数 `resolveUid(event.token)` 双端闭环；存量数据由 Phase 7 迁移映射。见缺口 G-01/G-02 |
| P0-2 云函数无鉴权 | 1.2 login、1.3 各函数入口 | ✅ | 7 个云函数逐一列明接入方式；`login` 接入 uni-id-common。`get_daily_quote` 公开、`schedule_notify` 定时器，判断合理 |
| P0-3 schema 权限全开 | 2.1 | ✅ | 12 个 schema 全部收紧为 false；经复核全前端仅 `search.vue:95` 一处客户端直查，方案已覆盖（3.3）。`index.vue:153` 的 `db` 声明未使用，无风险 |
| P0-4 日历打点字段错误 | 3.1 | ✅ | 修正为 `owner_id`，并纳入"我报名的日程"。属分析文档语义的小幅增强（原语义=收藏∪个人日程，新增"报名"红点），需与产品确认 |
| P0-5 搜索页缺陷 | 3.3 / 3.4 | ✅ | 修复调用、注册 pages.json、收藏搜索改云函数 |

### 2.2 P1 级问题

| 问题 | 方案章节 | 状态 | 核查说明 |
|---|---|---|---|
| P1-6 私有日程/名单/卡片越权 | 4.1/4.2/4.3 | 🟡 | detail/signups/flashcards 已覆盖；**`schedules.history` 仍无任何鉴权**（任何人凭 schedule_id 可读变更历史），见缺口 G-03 |
| P1-7 报名名额非原子 | 5.1 | 🟡 | 事务 + `signup_count` 原子字段方向正确；**但 `cancelSignup` 未同步递减 `signup_count`**，会导致计数漂移、满员状态错乱，见缺口 G-04 |
| P1-8 收藏/join/setRemark 非原子 | 5.2/5.3/5.4 | ✅ | 唯一索引 + 冲突捕获、条件 push、事务方案均合理 |
| P1-9 提醒发送失败丢提醒 | 5.5 | 🟡 | 思路正确，但表述需细化：应"按条标记成功"，而非"全部成功后写回"，见缺口 G-05 |
| P1-10 密钥硬编码 | 6.1 | ✅ | 天行 key/模板 ID/AppID 收敛至配置中心；前端今日诗词 token 处理见缺口 D-02 |

### 2.3 P2 级问题

| 问题 | 方案章节 | 状态 | 核查说明 |
|---|---|---|---|
| 全库无索引 | 2.2 | 🟡 | 16 条索引清单齐全；**唯一索引创建时机与迁移去重冲突**，见缺口 G-01 |
| schedules 单体 947 行 | 6.2 | ✅ | 拆子模块合理，长期可拆云函数 |
| groups 并发 | 5.3/5.4 | ✅ | join 条件 push、setRemark 事务 |
| 级联清理不完整 | 6.3 | ✅ | 补删 history/drafts，废弃 group_ids 决策合理 |
| 模板 ID / AppID 重复硬编码 | 6.1/6.6 | ✅ | 配置中心单一来源 |
| uni-id 配置未落地 | 1.2/6.1 | ✅ | 接入 uni-id-common，tokenSecret 强随机化 |
| daily_data 缓存无 TTL/多缓存 | 6.4 | ✅ | expireAt + 清理同日期多余 api_cache |
| 批量导入逐条 N 次 | 6.5 | ✅ | batchCreate |
| `cmd.in` 数量上限 | 3.1(备注) | 🟡 | 方案仅在 getMarks 处提示；**`schedules.attachSignupCounts`(index.js:329) 对最多 500 条日程执行 `cmd.in(ids)` 同样有超限风险**，见缺口 G-06 |
| N+1（checkFavoriteStatus 逐卡 check） | — | ❌ | 分析文档 P2 提到但方案未优化；建议补充 `toggle_favorite.checkBatch` 批量接口（低优先级，见建议 R-03） |

### 2.4 方案新增补充项（➕）

| 方案动作 | 评估 |
|---|---|
| `uni-id-token` 登录时清旧 token | ✅ 防表膨胀；但见缺口 D-01（JWT 吊销语义） |
| `schedules.signup_count` 字段 + 迁移回填 | ✅ 合理；但取消报名联动缺失（G-04） |
| `uni-id-users.birthday_mmdd` 派生字段 | ✅ 消除生日正则全表扫描 |
| 搜索页注册 + 云函数化 | ✅ |
| 游客写操作统一 401 | ✅ 行为收紧，符合预期 |

---

## 三、方案内部缺陷与遗漏（缺口清单）

### G-01【顺序缺陷】唯一索引创建时机与迁移去重冲突

- **现象**：方案 Phase 2.2 要求创建唯一索引（`user_favorites` 的 `(user_id,date,content)`、`signups` 的 `(schedule_id,user_id)`、`groups.invite_code`、`uni-id-users.openid`、`uni-id-token.token`），而 Phase 7 迁移才去重。
- **风险**：存量数据在迁移去重**之前**就已存在重复记录 → 创建唯一索引会**直接失败**；或先建索引会阻塞迁移写入。
- **整改**：调整顺序为「先做非唯一索引 → Phase 7 迁移去重 → 再去重完成后创建唯一索引」，或把 Phase 2.2 中的唯一索引项拆到 Phase 7 末尾执行。测试用例 T-701~T-705 顺序相应调整。

### G-02【边界】游客/半登录态空 `uid` 未明确

- **现象**：`getUid()` 在有 token 无 uid 时返回 `null`；从未登录也从未走游客登录时 `current_user_id` 为空 → `callApi` 传入 `uid:''`，云函数 `resolveUid` 对无 token 且非 visitor_ 前缀返回 401。
- **风险**：首次打开首页（未点游客模式）时收藏/打点行为不一致；部分页面（favorites.vue:84-86 原逻辑无游客兜底）行为变化。
- **整改**：明确三层身份约定并写入方案：①已登录→uid；②游客→`visitor_xxx`（由 `getUid` 兜底生成）；③无任何本地态→前端先走游客初始化或直接提示登录。`utils/auth.js` 的 `getUid()` 应保证永远返回非空字符串。

### G-03【遗漏】`schedules.history` 无鉴权

- **现象**：`schedules/index.js:640-648` 仅校验 `schedule_id`，任何人可读任意日程变更历史（含 before/after 快照）。
- **风险**：与 P1-6 同类越权，方案 4.1 只改了 `detail`，漏了 `history`。
- **整改**：`history` 复用 `detail` 的权限判断（private=owner，group=群成员）。补充测试用例 T-410。

### G-04【缺陷】`signup_count` 与取消报名/活动设置未联动

- **现象**：方案 5.1 报名时 `signup_count +1`，但 `cancelSignup`（schedules/index.js:771-803）与 `updateActivity`/`delete` 均未递减该计数。
- **风险**：计数漂移 → 满员自动关闭失效/名额被占死（如 quota=50，50 人报名后 20 人取消，仍显示满员）。
- **整改**：①`cancelSignup` 事务内 `signup_count -1` 并在低于 quota 时重新开放；②`delete` 前无需单独处理（整表删除）；③补充测试用例 T-503（原用例仅验证 reopening，需增加断言 `signup_count` 数值）。

### G-05【表述】提醒发送成功标记应为"按条"，非"全部成功后写回"

- **现象**：方案 5.5 写"逐条发送、**全部成功后**再写回"；又写"失败不置 sent"。两者在部分成功时会互相矛盾：全部成功后写回会把失败项也一并覆盖。
- **整改**：明确为「每条提醒独立判定：`sendMsg` 返回成功才将该条 `sent=true` 并更新 `next_remind_at`；失败的保持原状待下轮重试」。对应测试用例 T-507 已按此设计，无需改。

### G-06【遗漏】`attachSignupCounts` 的 `cmd.in` 超限

- **现象**：`schedules/index.js:329` 对 `getMonth` 最多 500 条日程的 `ids` 执行 `db.command.in(ids)`；uniCloud 的 `in` 运算元素数量存在上限（约 100 级），超过会报错/截断。
- **风险**：用户某月日程/群日程较多时，报名数统计失效甚至 getMonth 报错。
- **整改**：方案 3.1 的"分批 `cmd.in`"提示应推广到 `attachSignupCounts`（对 ids 每 100 一批循环查询），或改用 `signups` 聚合（`aggregate().match().group`）一次取回计数。补充测试 T-511（造 150+ 日程验证统计完整）。

---

## 四、需决策确认项

### D-01【决策】JWT 删除 DB 记录 ≠ 真正吊销

- **事实**：uni-id-common 的 `checkToken` 是无状态 JWT 校验（签名+过期），**不查询 `uni-id-token` 表**。
- **含义**：方案 1.2"登录时清理旧 token"只解决表膨胀，**已签发的旧 token 在 30 天（tokenExpiresIn）内仍有效**；泄露 token 无法即时吊销。
- **选项**：A) 接受（微信小程序用户数有限，风险可容忍，JWT 短过期）；B) 需要强吊销时，`resolveUid` 在校验 JWT 后再查一次 `uni-id-token` 是否存在该 token（牺牲一点性能换取可吊销）。
- **建议**：默认选 A，在方案中明确说明"清旧 token 为防膨胀、非吊销"；如产品有封号需求再升级 B。

### D-02【决策】今日诗词改云函数代理与既定架构冲突

- **事实**：`开发方案思考.md` 明确「今日诗词 = 前端直调 + Token」，方案 6.1 改为云函数代理。
- **影响**：前端 `fetchJinrishici` 与 `get_daily_quote` 的 `Promise.all` 并行结构需调整，属架构文档同步修改项。
- **建议**：确认接受架构变更；若保持直调，则接受小程序包内 token 暴露的现实（今日诗词 token 本身为公开可申请，风险低），仅在方案中保留提示。

---

## 五、补充建议（低优先级，可并入后续迭代）

| 编号 | 建议 | 依据 |
|---|---|---|
| R-01 | 收藏唯一索引改用 `content_hash` 字段（SHA256）替代长文本 `content` | MongoDB 复合索引 key 有长度上限，新闻/历史长文可能超限导致插入失败；hash 字段（32 hex）安全且可建唯一索引 |
| R-02 | `groups.create` 邀请码冲突重试（唯一索引 + 循环重生成） | 建唯一索引后碰撞由"低概率"变为"必然抛错"，需 catch 后重试（方案 5.x 未提及） |
| R-03 | `toggle_favorite` 增加 `checkBatch`，消除前端逐卡 N+1 | 分析 P2 遗留项，首页收藏状态批量核对 |
| R-04 | 云函数鉴权抽公共模块（`common/uni-verify`），避免 5 处重复 `resolveUid` | 方案 1.3 注明"本地函数，最小侵入"，长期应复用公共模块降低漂移 |
| R-05 | `uni-id-token` 定期清理过期行（并入 schedule_notify 或定时任务） | 配置了 tokenExpiresIn，但从未落地清理策略 |
| R-06 | `schedule_notify` 增加并发互斥（如云函数实例单跑开关/锁字段），防 cron 重叠双发 | 方案 5.5 未提，配合 T-508 防回归 |

---

## 六、测试用例与方案闭合情况映射

| 方案验收点 | 测试用例 | 覆盖 |
|---|---|---|
| 身份稳定/鉴权 | T-101~T-109 | ✅ 全覆盖 |
| schema 封闭 + 索引 | T-201~T-204 | ✅（唯一索引创建时机调整后有效，见 G-01） |
| 打点/搜索/私有权限 | T-301~T-307、T-401~T-409 | 🟡 缺 history 用例（G-03 补充 T-410） |
| 并发一致性 | T-501~T-506 | 🟡 T-503 需增加 signup_count 断言（G-04） |
| 提醒可靠性 | T-507~T-510 | ✅（按 G-05 表述修正） |
| 配置/拆分/批量 | T-601~T-604 | ✅ |
| 迁移 | T-701~T-705 | 🟡 需与 G-01 顺序对齐 |
| 回归 | T-801 | ✅ |

---

## 七、最终结论

1. **方向正确、可执行**：方案完整覆盖了分析文档 4 个 P0 与 5 个 P1 的核心诉求，架构决策（uid 统一身份、uni-id-common、schema 全封闭、事务化、配置中心）均合理。
2. **建议整改后实施**：先修复 3 处方案缺陷（G-01 顺序、G-04 取消报名计数、G-05 标记粒度）与 3 处遗漏（G-03 history 鉴权、G-06 cmd.in 分批、G-02 游客边界），并确认 D-01/D-02 两项决策。
3. **测试方案配套**：`整改方案测试方案与用例.md` 可作为验收依据，按 G-01/G-03/G-04/G-06 补充对应用例后即可作为回归基线。
4. **无阻断性问题**：未发现方案会放大现有漏洞或引入比现状更严重的新风险。

> 本报告基于静态代码与平台能力核查；事务/并发用例（T-501~T-506）与迁移用例（T-701~T-705）需在开发空间实测后给出最终结论。
