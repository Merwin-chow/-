# 云函数调用模式·定时设置·缓存设计 专项评估报告

> 版本：v1.0 ｜ 评估日期：2026-08-16
> 评估对象：全量云函数（`login`/`get_daily_quote`/`toggle_favorite`/`flashcards`/`groups`/`schedules`/`schedule_notify`）+ 前端页面调用 + 定时触发器 + `daily_data` 缓存
> 方法：逐函数阅读源码，重点核查三层设计——①前端→云函数调用模式（并发/冗余/重复请求）；②`schedule_notify` 定时触发器（cron 表达式/执行窗口/互斥/分批）；③`daily_data` 缓存（生命周期/多文档/孤儿/过期）。
> 在前期《整改方案检查报告》《整改方案可行性综合评估报告》基础上作增量覆盖，并集中回应其中遗留问题。

---

## 一、结论速览

| 维度 | 总体评价 | 遗留问题 | 本报告对应章节 |
|---|---|---|---|
| 云函数调用模式 | 🟡 基本合理，存在 N+1、全量拉取、重复调用、错误吞噬 | 检查报告 R-03、可行性报告 M-12 | §二 |
| 定时设置 | 🟡 可实现但存在并发重入、分批遗漏、时区窗口等隐患 | 可行性报告 M-11（R-06） | §三 |
| 缓存设计 | ❌ 存在当日缓存永不过期、孤儿文档、forceRefresh 副作用、冗余读等 | 检查报告 6.4、可行性报告 M-2/M-10 | §四 |

### 遗留问题解决对照
| 之前报告遗留问题 | 本报告落地方案 |
|---|---|
| 检查报告 R-03（收藏 N+1 `checkFavoriteStatus`） | §2.1 新增 `checkBatch` 批量接口 + 前端单次调用 |
| 检查报告 R-06 / 可行性报告 M-11（定时器并发互斥） | §3.2 云函数锁 + **cron 分隔窗口**双保险 |
| 可行性报告 M-10 / M-2（缓存策略） | §4 完整缓存 TTL 方案 + 多缓存消除 + forceRefresh 修正 |
| 检查报告 6.4（`daily_data` 多缓存孤儿） | §4.2 upsert 前清理 + 当日唯一索引 |
| 可行性报告 M-13（鉴权公共模块 `common/uni-verify`） | 本报告 §2.3 一并纳入（与调用模式强相关） |

---

## 二、云函数调用模式评估

### 2.1 前端→云函数调用审计

**已核实的调用点**（前端源码 `pages/*` 与编译产物 `.js`）：

| 页面 | 调用 | 模式评估 |
|---|---|---|
| `index/index.vue` | `loadData` 内 `Promise.all([fetchJinrishici(), callFunction(get_daily_quote)])` | ✅ 并行合理；但三者独立无兜底链，见风险 R1 |
| `index/index.vue` | `checkFavoriteStatus`：`for` 循环逐个 `toggle_favorite` `action:'check'` | ❌ **N+1 问题**。每张卡片一次云函数调用，卡片多时串行阻塞 |
| `index/index.vue` | `loadData` + `loadCalendarMarks` 在 `onMounted` **并行同时发起** | ⚠️ 收藏 `add` 与 `getMarks` 会在收藏动作后手动再调一次 `loadCalendarMarks()`，见风险 R2 |
| `index/index.vue` | `onMonthSwitch` → `loadCalendarMarks` | ⚠️ 切月全量拉取所有收藏+日程日期（`getMarks` 无 month 参数） |
| `favorites/index` … | `toggle_favorite` `list` | ⚠️ 列表无分页，条数多时一次性拉全量 |
| `schedule/schedule.vue` | `onMounted` → `schedules.getMonth` | 📧 getMonth 内部 `baseLoad` 对「个人+群」两组、`attachSignupCounts`、`attachMyStatus` 最多 3 次 DB 往返，且 `cmd.in` 有 500 条上限 |
| `schedule-detail.vue` | `detail`、`signups`、`history` 多次调用（其中 `group-detail` 校验是否群主） | ⚠️ 详情页 3~5 次云函数调用，可合并 |
| 各页面通用 `getUserId()` | 每次渲染都读 storage | ✅ 本地无网络，可接受 |
| `groups`、`flashcards` 各 action | 一次一动作 | ✅ |

**风险清单 R1~R6**：

- **R1【高】`checkFavoriteStatus` N+1 + 无缓存**：首页每切一天、每进一次都逐卡 `check`，卡片来自 3 个数据源切面，多次进入重复全量 check。缓解：`toggle_favorite` 增加 `checkBatch`（传 `content[]` 数组，一次返回 `{content: isFavorited}` 映射），前端改用单次 `Promise.all([get_daily_quote, checkBatch])`。
- **R2【中】收藏动作后重复刷新**：`toggleFavorite` 成功后既本地更新 `isFavorited` 又调用 `loadCalendarMarks()` 全量重拉 getMarks，打点数据可局部更新（`add` 返回时直接 push 日期到 `calendarMarks`），避免整表重拉。
- **R3【中】`getMarks`/`getMonth` 全量拉取**：无 month 参数，历史数据累积后每次拉全量。落地：`getMarks` 增加 `month`，按 `date gte/lte` 过滤；`getMonth` 已在按 month 过滤但对群日程 `cmd.or(gids.map())` 在大群量下仍需分批（见 G-06 关联）。
- **R4【中】错误静默 + 无统一返回**：`flashcards`/`groups` 等 `sendSubMsg` `.catch(()=>{})` 吞错；`get_daily_quote` 外部 API 单点失败仅 console 日志。落地：统一异常出口（返回 `code/500` + traceId），订阅发送失败写入重试队列。
- **R5【低】`updateProfile`/`phoneLogin` 信任客户端**：见既有 P0-2（identity 断裂），本报告不重复，仅强调调用侧需在 `resolveUid` 落入。
- **R6【低】`schedules.search` limit 500 内存过滤关键词**：卡片 >500 结果不全；落地：数据库建立全文/正则索引或分页。

### 2.2 云函数→数据库往返（读放大）

`baseLoad`（schedules）在单个用户查询时已触发 **1（日程）+ 1（报名数 in）+ 1（我的报名）+ N（群成员校验）** 多次 DB 往返；`attachSignupCounts` 用 `cmd.in(ids)` 撞 uniCloud `in` 元素上限（既有 G-06 已识别）。

**落地**（汇总可行性报告 M-7）：
- `attachSignupCounts` 改为 `signups.aggregate().match({schedule_id:in}).group({_id:'$schedule_id',n:sum(1)})`，一次聚合取全部计数。
- `baseLoad` 的 `attachMyStatus` 与 `attachSignupCounts` 可合并为**一次聚合**同时取「报名数 + 我的状态」，减少一次往返。
- 群成员校验 `isGroupMember` 对同一群多次调用时，应在函数内缓存 `gid->members` 结果（per-invocation 级缓存），消除循环内重复读群。

### 2.3 调用侧公共抽象（关联可行性 M-13）

当前 7 个云函数各自 `exports.main` 无公共鉴权模块。建议新建 `cloudfunctions/common/uni-verify/`（以 `schedules` 已验证的 `file:../common/uni-subscribemsg` 方式被各函数依赖），暴露 `resolveUid(event)`，同时承载统一错误码与日志。此改动把「鉴权、错误码、日志」收敛，直接降低 §2.1 各调用点的重复代码与漂移。

---

## 三、定时设置评估（`schedule_notify`）

### 3.1 现有配置

`schedule_notify/package.json`：
```json
"cloudfunction-config": {
  "concurrency": 1,
  "memorySize": 128,
  "timeout": 10,
  "triggers": [
    { "name": "schedule_notify_timer", "type": "timer", "config": "0 */5 * * * * *" },
    { "name": "birthday_notify_timer", "type": "timer", "config": "0 0 9 * * * *" }
  ]
}
```

### 3.2 定时设计缺陷与整改

**T1【高】cron 表达式与 `runScheduleRemind` 分批逻辑冲突可能丢提醒**
- `schedule_notify` 每 5 分钟触发一次，但 `runScheduleRemind` 每次 `.limit(100)` 只处理前 100 条。
- 若某轮到期日程 >100，本应下一轮（≥5 分钟后）补完；但 `next_remind_at` 在**处理完才统一更新**，未处理的 100+ 条若 `fireTime` 已过但 `now < schedule_time`，会在下一轮重新命中（可补，**不丢**）；若已过期则被清成 `sent:true` 并 `next_remind_at:0`，**中间态的**批次不重发——存在边界遗漏。
- **落地**：`runScheduleRemind` 增加排序 `orderBy('next_remind_at','asc')`，保证同轮先处理最早到期；同时每轮处理上限从 100 提升并循环直到拉空（`while` 循环 + 游标），配合下一轮 cron 双保险。

**T2【高】跨 cron 重复执行（重入）**
- `concurrency:1` 只限制**同一函数实例并发的实例数**，并不能保证 5 分钟 cron 与前一轮未跑完不交叠。
- **落地（M-11 落地）**：在 `schedules` 集合写入运行锁（`notify_lock` 文档：`{ state:'running', ts }`），执行前 `add` 幂等占锁，开始即 `set` 获取；结束释放；对陈旧锁（上一轮超时）做 `cmd.lte(now-timeout)` 抢占。
- **双保险（新增）**：把「日程提醒」与「生日提醒」拆分到**两个独立的触发器**（cron 错峰：日程每 5 分钟、生日每天 9:00），避免单函数内 `if/else` 判断 `triggerName` 带来的调度耦合（当前 `onlyBirthday || !triggerName` 分支会让手工调用同时跑生日与日程，且当天 9:00 窗口 `!triggerName` 会触发生日——用途不明易误发）。

**T3【中】生日触发器时区/窗口**
- `birthday_notify_timer` cron `0 0 9 * * * *`（UTC 每天 9 点）→ 实际为北京时间 17 点，与代码里「当天 9 点窗口 `hour===9 && minute<5`」的 UTC 判断**不一致**：cron 触发点（UTC 9 点=北京 17 点）永远不落入 `hour===9` 的代码窗口 → **生日通知只有手工调用才发，定时器形同虚设**。
- **落地**：统一时区基准。cron 改为北京时间（阿里云 uniCloud cron 以 UTC 计）或代码窗口改为匹配 cron 实际触发时刻；并在注释明确「今天生日」以 `todayCn(+8)` 计算。

**T4【低】`timeout:10` 秒对批量发送过紧**
- `sendMsg` 串联多名接收者（群日程/生日群成员可能几十人），10 秒可能超时中断。`memorySize 128MB` 偏小。
- **落地**：timeout 提至 30~60s、memorySize 提至 256MB；发送改为并发（`Promise.allSettled`，限制并发数如 5），既提速又避免超时。

**T5【低】发送失败重试缺失**
- `sendMsg` 失败仅返回 `false`，不落库待重发 → 用户"授权了却没收到"。落地（关联可行性 M-5/M-11）：成功才置 `sent:true`（既有 G-05）；失败写入 `notify_failed` 重试集合，由下一轮从该集合补发（限重试次数）。

### 3.3 定时设计总表

| 触发器 | 现 cron | 现状 | 整改后建议 |
|---|---|---|---|
| 日程提醒 | `0 */5 * * * * *`（5min） | 分批 + 无边检重入 + 时效窗口不排序 | 保持 5min，加排序 + 循环拉空 + 运行锁 + 错峰 |
| 生日提醒 | `0 0 9 * * * *` | **时区错位，定时器不生效** | 修正时区基准 + 独立触发器 |

---

## 四、缓存设计评估（`daily_data`）

### 4.1 现有实现（`get_daily_quote/index.js`）

- 按 `date` 查询 `daily_data`，`source==='api_cache'` 且 `items` 非空视为命中；命中且非 `forceRefresh` 直接返回缓存。
- 未命中则 `fetchFromApis`（hitokoto + 天行 AI/新闻/历史 4 源 `Promise.all`），写 `daily_data`（首条写 `quote`/`author`，全量写 `items`）。
- 手动文档（`source!=='api_cache'`）无条件叠加在最前；`?question/answer` 仅手动文档有意义。
- 前端 `index.vue` 另直调今日诗词（`fetchJinrishici`），与云函数内容并列。

### 4.2 缓存缺陷与整改

**C1【高】当日缓存永不过期 + 无 TTL**
- `daily_data` 无 `expireAt`，一旦首抓即永久固化；当日 AI 文案/新闻/历史当天后续不再刷新（历史语义存疑）。
- **落地（响应可行性 M-10/M-2）**：
  - 增加 `expireAt`（当日缓存 `date T+24h`）命中前判断。
  - 当日缓存**到点后自动回源**（强制刷新当天的缓存）；历史日期维持「时光机」保留可读。
  - 由 `schedule_notify`（或独立 `daily_data_refresh` 定时器）在每日切换时主动刷新当天内容，避免靠用户触发。

**C2【高】`forceRefresh` 副作用——不回源却标 cached**
- 当 `forceRefresh:true` 且命中缓存文档时，`if(cacheDoc && !forceRefresh)` 不成立 → 走 `fetchFromApis` 并 `upsertApiCache` 回写；但**返回值却用 `cacheDoc.items` 判断 cached 标志**，逻辑混乱且 `cached:true` 可能伴随老数据。
- **落地**：`forceRefresh` 语义重定义为「强制回源并写缓存」，命中分支与回源分支的返回值/`cached` 标志严格一致。

**C3【中】多 `api_cache` 文档孤儿**
- `upsertApiCache` 只 upsert 找到的第一个 `cacheDoc`，若当天已存在多份 api_cache（并发追写入/异常中断），其余成为孤儿永不被更新。
- **落地（关联检查报告 6.4）**：upsert 前先 `daily_data.where({date, source:'api_cache'}).remove()` 清理同日期残留，再写入；并在 `date` 建唯一索引兜底。

**C4【中】`get_daily_quote` 每次全表按 date 查 + 嵌套 items 大文档**
- 当前命中逻辑每请求 `where({date}).get()` 拉整个 items 数组（可能几十条），对「只看当日一言」场景浪费带宽/计算。
- **落地**：命中时按需 `field` 裁剪（不必每次拉全部 items）；`date` 加索引（既有 2.2 已列）。
- 补充：items 内嵌套 fields 应加 schema 白名单约束（`daily_data.schema.json` 已部分约束）。

**C5【低】缓存与手动配置同集合语义混杂**
- `daily_data` 同时承载「api_cache 缓存」与「手动编辑推荐」（question/answer、manual source）。建议拆分两个集合（`daily_cache` / `daily_manual`）或至少用 `type` 字段分离，降低运维误操作。

### 4.3 前端缓存缺失
- 首页每次进入都 `fetchJinrishici` + `get_daily_quote` 全量回调；浏览器/小程序无本地缓存策略。
- **落地**：前端 `storage` 缓存当日一言（key 含 `date`），`onMounted` 先取缓存秒出，后台刷新替换，减少冷启动请求与闪白。

---

## 五、遗留问题与处置对照表

| 出处 | 遗留点 | 本章落地 | 验收用例 |
|---|---|---|---|
| 检查报告 R-03 | 首页收藏状态逐卡 N+1 | §2.1 `checkBatch` 批量接口 + 前端单次并行 | 新增 `toggle_favorite.checkBatch` 测试：传 10 条 content，1 次返回；首页进入仅 1 次 checkBatch |
| 检查报告 R-06 / 可行性 M-11 | 定时器并发重入 | §3.2 T2 运行锁 + cron 分隔 | T-508 升级：并发触发两次 schedule_notify，仅一轮产生提醒 |
| 可行性 M-10 / M-2 / 检查 6.4 | 缓存 TTL / 孤儿 / forceRefresh | §4.2 C1/C2/C3 | 新增缓存用例：今日过期回源、forceRefresh 正确回源、同日期仅 1 api_cache 文档 |
| 可行性 M-7（G-06） | `cmd.in` 超限 | §2.2 聚合替换 | T-511 造 150+ 日程验证计数完整 |
| 可行性 M-13 | 鉴权无公共模块 | §2.3 `common/uni-verify` | 复用 T-101~T-109 回归 |
| 既有 P0-2 | identity 断裂 | §2.1 R5（调用侧约束） | T-106 越权用例 |

---

## 六、最终结论

1. **调用模式**：以「单一云函数 + action 路由 + `daily_data` 缓存」的主干是合理的，但存在首页收藏 N+1（R1）、getMarks/getMonth 全量拉取（R3）、订阅错误吞噬（R4）、`cmd.in` 上限（G-06 关联）等需收敛的点，且缺公共鉴权/日志（§2.3）。
2. **定时设置**：最大问题在**生日定时器时区错位导致实际上不触发（T3）** 与 **跨 cron 重入、分批边界遗漏（T1/T2）**。建议拆独立触发器、修正时区、加运行锁、排序+循环拉空、并适度放宽 timeout/memory。
3. **缓存设计**：`daily_data` 无 TTL、`forceRefresh` 语义混乱（C2）、多缓存孤儿（C3）、前端无本地缓存均需落地；拆分缓存与手动配置集合可根本降低运维风险。
4. **遗留问题闭环**：§五 将前期检查报告与可行性报告中的缓存/定时/调用相关遗留项全部给出可验收的落地方案，并结合 §四 的测试用例作为回归基线。
5. **建议**：在实施《整改方案》Phase 3~6 时，把本报告 §2/§3/§4 的整改项并入对应 Phase；其中 T3（生日时区）、C2（forceRefresh）与 R1（收藏 N+1）建议列为 P0，其余 P1/P2。

> 本报告为专项评估，未修改任何代码或文档；所有建议均给出落点、优先级与对应验收用例，供并入既有《整改方案》实施。
